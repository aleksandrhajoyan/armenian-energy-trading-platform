# Agents — AI Energy Trading Platform

Compact specification for all 13 agents. Names in this file are canonical. Do not rename or invent aliases in code or docs.

**Status:** specification of agent responsibilities remains in this file. Canonical input/output contracts are implemented in `energy_trading.domain.models`. Application owns shared identity and invocation types: `AgentName` (exactly these 13 display names) and generic `AgentPort[TRequest, TResult]`. The shared verb is `async run(request)`. Six concrete application agents now exist: Weather & Renewable Forecast Agent, Hydro Resources Agent, Generation Availability Agent, News Intelligence Agent, Market Monitoring Agent, and Regulatory Intelligence Agent. Weather, Hydro, Generation Availability, News Intelligence, Market Monitoring, and Regulatory Intelligence application boundaries are implemented. Application-owned `RegulatoryIntelligenceQueryExecutionService` composes query-text preparation with the unchanged Regulatory Intelligence Agent. An explicit outer composition root (`build_regulatory_intelligence_query_execution`) can assemble that stack from three injected application ports. A provider-aware API composition function (`build_regulatory_intelligence_provider_runtime`) can construct `OpenAIDocumentQueryEmbeddingAdapter`, `QdrantDocumentVectorSearch`, and `OpenAIRegulatoryConstraintInferenceAdapter` from injected `AsyncOpenAI` / `AsyncQdrantClient`, existing `QdrantDocumentVectorConfig`, and two explicit model strings, then delegates to that provider-neutral builder. A configured API composition function (`build_regulatory_intelligence_configured_runtime`) can assemble that provider-aware stack from injected clients plus already-constructed `RegulatoryIntelligenceRuntimeSettings` by mapping collection name and vector size into `QdrantDocumentVectorConfig` and forwarding the two model fields. A managed API composition function (`managed_regulatory_intelligence_runtime`) can own provider-client lifetime from already-loaded `OpenAISettings`, `QdrantSettings`, and `RegulatoryIntelligenceRuntimeSettings` by creating clients through existing factories, delegating to the configured builder, yielding the query-execution service, and closing both clients. A settings-loaded API composition function (`loaded_regulatory_intelligence_runtime`) can load those three typed settings through the existing loaders and delegate to that managed runtime. A FastAPI-compatible lifespan factory (`build_regulatory_intelligence_lifespan`) can enter and exit that settings-loaded runtime for the duration of an application lifespan; while active it stores the exact yielded service on `app.state.regulatory_intelligence_query_execution_service` and removes that reference before Chunk 75 teardown. Production `create_app()` installs that callback by default. A typed API accessor (`get_regulatory_intelligence_query_execution_service`) can read that exact lifespan-scoped service from a request's application state without invoking it. A dedicated HTTP query router uses production-style `Depends(...)` on that accessor, invokes the published query-execution service, and maps canonical constraints to the existing HTTP DTO; production `create_app()` installs that router under the existing API prefix so `POST /api/v1/regulatory-intelligence/query` is reachable. Construction remains lazy; execution still depends on the lifespan-scoped service; offline/test lifespans can still avoid provider credentials. API-owned HTTP transport DTOs (`RegulatoryIntelligenceQueryRequest`, `RegulatoryIntelligenceQueryResponse`) project canonical query input and constraint results. Typed `RegulatoryIntelligenceRuntimeSettings` now holds those two model identifiers plus Regulatory Qdrant collection name and vector size under `ENERGY_REGULATORY_*`, separately from `OpenAISettings` and `QdrantSettings`; it is loaded by `loaded_regulatory_intelligence_runtime`; it is not loaded by the provider-aware builder, the configured builder, the managed runtime, either adapter, the Chunk 67 builder, `create_app()`, or LangGraph. A concrete infrastructure OpenAI query-embedding adapter (`OpenAIDocumentQueryEmbeddingAdapter`) structurally implements `DocumentQueryEmbeddingPort`; it injects `AsyncOpenAI` and an explicit model and does not construct the client or wire `create_app()` / LangGraph. A concrete infrastructure OpenAI regulatory-constraint inference adapter (`OpenAIRegulatoryConstraintInferenceAdapter`) structurally implements `RegulatoryConstraintInferencePort`; it injects `AsyncOpenAI` and an explicit model, converts structured provider output into canonical `RegulatoryConstraint`, and does not construct the client or wire `create_app()` / LangGraph. An offline typed OpenAI client-construction foundation exists (`OpenAISettings`, `load_openai_settings`, `create_openai_client`) and is used by the managed runtime; `load_openai_settings` is used by the settings-loaded runtime; it is not used by the provider-aware builder, the configured builder, either adapter, the Chunk 67 builder, `create_app()`, or LangGraph; HTTP invocation through production `create_app()` now reaches the published query router; operational RAG remains absent; LangGraph wiring of the Regulatory service remains absent. The other 7 concrete agents remain unimplemented. Request and result contracts remain agent-specific. Application also owns a framework-neutral workflow snapshot (`WorkflowPhase`, `WorkflowStatus`, `WorkflowState`), a LangGraph runtime (`build_workflow_graph(*, parallel_ingestion_step: ParallelIngestionWorkflowStep, parallel_ingestion_failure_runtime_handler: ParallelIngestionFailureRuntimeHandlingService)`, `START → workflow_entry → parallel_ingestion`, then `ingestion`/`running` → `parallel_ingestion_success_transition` → `END` or `ingestion`/`failed` → `END`), a framework-neutral failure-policy hook (`FailureAction`, `FailurePolicyContext`, `FailurePolicyPort`), a framework-neutral parallel-ingestion fan-out plan (`ParallelIngestionPlan`) that composes the five existing typed Phase 2 request DTOs, a framework-neutral parallel-ingestion execution boundary (`ParallelIngestionExecutionPort`) with `async execute(plan) -> ParallelIngestionSuccess`, a framework-neutral all-five-success fan-in aggregate (`ParallelIngestionSuccess`) that composes the five existing typed Phase 2 result DTOs, a concrete LangGraph-free concurrent executor (`ConcurrentParallelIngestionExecutor`) that injects the five application agents and runs them with `asyncio.TaskGroup`, a framework-neutral parallel-ingestion workflow-context boundary (`ParallelIngestionWorkflowContextPort`) with `async resolve_plan(workflow_id) -> ParallelIngestionPlan` and `async record_success(workflow_id, success) -> None`, a framework-neutral Phase 2 workflow step (`ParallelIngestionWorkflowStep`) that composes resolve → execute → record and returns the original `WorkflowState` unchanged, a framework-neutral successful Phase 2 control-state transition (`advance_after_parallel_ingestion`) that maps `ingestion`/`running` to `forecasting`/`running` on a new `WorkflowState`, a framework-neutral terminal Phase 2 failure transition (`fail_parallel_ingestion`) that maps `ingestion`/`running` to `ingestion`/`failed` on a new `WorkflowState` without mutating diagnostics, a Phase-2-specific failure-policy decision boundary (`ParallelIngestionFailureDecisionService`) that accepts an already-constructed `FailurePolicyContext`, delegates exactly once to `FailurePolicyPort.decide`, and returns the published `FailureAction` unchanged, a Phase-2-specific failure-policy context constructor (`build_parallel_ingestion_failure_policy_context`) that builds that published context from already-sanitized typed failure facts without inspecting exceptions or inventing an agent identity, and a Phase-2-specific failure-action executor (`execute_parallel_ingestion_failure_action`) that applies terminal `FailureAction.FAIL` by delegating to `fail_parallel_ingestion` and rejects `RETRY` / `FALLBACK` as not implemented, and a Phase-2-specific prepared failure-handling composition (`ParallelIngestionFailureHandlingService`) that awaits `ParallelIngestionFailureDecisionService.decide(context)` exactly once and forwards the returned action to `execute_parallel_ingestion_failure_action`, and Phase-2-specific agent-failure attribution (`ParallelIngestionAgentFailure`) at the concurrent executor boundary, and Phase-2-specific ExceptionGroup attributed-leaf extraction (`extract_parallel_ingestion_agent_failures`), and Phase-2-specific sanitized one-leaf failure classification (`classify_parallel_ingestion_agent_failure` / `ParallelIngestionFailureFact`), and Phase-2-specific tuple-level failure-fact classification (`classify_parallel_ingestion_agent_failures`), and a Phase-2-specific failure-fact selection contract (`ParallelIngestionFailureSelectionPort`), and a Phase-2-specific strict single-failure selector (`StrictSingleParallelIngestionFailureSelector`), and a Phase-2-specific attempt-number source contract (`ParallelIngestionAttemptNumberPort`), and a Phase-2-specific initial attempt-number source (`InitialParallelIngestionAttemptNumberSource`), and a Phase-2-specific failure-policy context resolution service (`ParallelIngestionFailureContextResolutionService`), and a Phase-2-specific failure-context preparation service (`ParallelIngestionFailureContextPreparationService`) that accepts workflow ID, phase, and `BaseExceptionGroup`, then delegates extraction → tuple classification → context resolution into `FailurePolicyContext` without invoking policy or executing an action, and a Phase-2-specific initial terminal-fail failure policy (`InitialParallelIngestionFailurePolicy`) that structurally satisfies `FailurePolicyPort` and returns exactly `FailureAction.FAIL` for every valid context without inspecting fields or executing the action, and a Phase-2-specific runtime failure-handling composition (`ParallelIngestionFailureRuntimeHandlingService`) that accepts `WorkflowState` plus `BaseExceptionGroup`, delegates to `ParallelIngestionFailureContextPreparationService`, then delegates to `ParallelIngestionFailureHandlingService`, and returns the resulting `WorkflowState`. The graph invokes that application Phase 2 workflow step, catches only `BaseExceptionGroup` at that boundary, and delegates the exact group plus original `WorkflowState` to the injected `ParallelIngestionFailureRuntimeHandlingService`. On success it applies `advance_after_parallel_ingestion` in node `parallel_ingestion_success_transition`. The graph does not call `fail_parallel_ingestion`, `ParallelIngestionFailureDecisionService`, `build_parallel_ingestion_failure_policy_context`, `ParallelIngestionFailureContextResolutionService`, `execute_parallel_ingestion_failure_action`, `ParallelIngestionFailureHandlingService`, `extract_parallel_ingestion_agent_failures`, `classify_parallel_ingestion_agent_failure`, `classify_parallel_ingestion_agent_failures`, `ParallelIngestionAttemptNumberPort`, `StrictSingleParallelIngestionFailureSelector`, or `InitialParallelIngestionAttemptNumberSource`, or `ParallelIngestionFailureContextPreparationService`, or `InitialParallelIngestionFailurePolicy`. Direct non-group Phase 2 exceptions still propagate. The executor attributes ordinary agent failures as `ParallelIngestionAgentFailure` before TaskGroup aggregation; attributed-leaf extraction from that aggregation now exists; sanitized one-leaf classification now exists; tuple-level sanitized classification now exists; a Phase-2-specific selection contract now exists; a Phase-2-specific attempt-number source contract now exists; a Phase-2-specific failure-context resolution service now exists; a strict single-failure selector now exists for the unambiguous one-fact case; simultaneous multi-failure selection remains unresolved; there is no first/last/priority winner rule; an initial attempt-number source now exists that always returns `1`; there is no retry-capable attempt tracker and no increment/reset semantics; a Phase-2-specific failure-context preparation service now exists that composes extraction → classification → context resolution; the preparation service is callable only when a constructed resolution service is injected; the strict selector and initial attempt source exist and can be injected behind that resolution service, but are not auto-wired; an initial terminal-fail failure policy now exists that always returns `FailureAction.FAIL` and is not auto-wired into preparation, handling, or LangGraph; a Phase-2-specific runtime failure-handling composition now exists that composes preparation plus existing handling from `WorkflowState` plus `BaseExceptionGroup` and is injected into LangGraph at the Phase 2 step boundary; retry-capable attempt tracking does not; simultaneous multi-failure resolution and attempt tracking still do not. The step delegates through the execution port and the existing concurrent executor only when a caller composes those dependencies. A process-local in-memory `InMemoryParallelIngestionWorkflowContext` exists as unwired local/dev infrastructure. There is no production/durable workflow-context implementation, no concrete Chief Orchestrator, no actual five-phase routing, no Phase 3 execution, no Phase 2 join for failure/degraded cases, no partial/degraded fan-in policy, no LangGraph wiring of lower-level failure internals, no retry/fallback execution, no runtime composition of the initial failure policy with action execution inside the graph factory, and no prompt/tool/model catalog.

Agents live in the application layer. They consume and return canonical typed contracts (`DATA_CONTRACTS.md`). They never import concrete infrastructure adapters or concrete ML implementations; they depend on application ports. The composition root injects implementations from `infrastructure` and `ml`.

**Ingestion boundary:** Ingestion agents must consume adapter/application ports that return canonical records. They must never parse CSV, Excel, vendor JSON, or raw API schemas directly.

## Agent kinds

| Kind | Meaning |
| --- | --- |
| LLM / reasoning | Uses a language model over canonical text/constraints. Must not emit numerical forecasts. |
| Deterministic service | Rules, arithmetic, and I/O via ports. No LLM required for the core result. |
| ML-backed | Calls forecasting services through injected application ports implemented by `ml`. An LLM must not calculate the forecast. Agents must never import XGBoost, LightGBM, Prophet, or concrete model classes. |
| Orchestration | Owns workflow graph, state, retries, fallback. Thin nodes only. |

Hybrid agents may attach an LLM **narrative** to a deterministic or ML result. The numerical payload remains owned by the non-LLM path.

External tools listed below are **capability classes**, not vendor products. Concrete API providers are not chosen in this chunk.

---

## 1. Regulatory Intelligence Agent

- **Kind:** LLM / reasoning (RAG)
- **Purpose:** Interpret market rules, licenses, and regulatory documents into structured constraints the rest of the workflow can enforce.
- **Canonical inputs:** retrieved document chunks (infrastructure-normalized), `RegulatoryConstraint` drafts, workflow/correlation ID, delivery-day window.
- **Canonical outputs:** `RegulatoryConstraint` set; `AdapterDiagnostic` as needed.
- **External tools/systems (eventual):** unstructured document adapters, OCR/extraction port, Qdrant retrieval port, LLM provider port.
- **Dependencies:** Chief Orchestrator Agent (invocation); unstructured ACL; Qdrant. Does not call ML forecast modules.
- **Failure behavior:** If retrieval or LLM inference fails, emit diagnostics, keep previously valid constraints if the orchestrator policy allows, and never invent enforceable numeric limits. Unreadable documents follow the unstructured ACL / DLQ path.
- **Implemented now:** typed application request/result (`RegulatoryIntelligenceRequest`, `RegulatoryIntelligenceResult`); concrete application agent (`RegulatoryIntelligenceAgent`); existing document vector search port dependency (`DocumentVectorSearchPort` / `DocumentVectorSearchQuery`); application-owned regulatory-constraint inference port (`RegulatoryConstraintInferencePort`). The agent still accepts an already-constructed `DocumentVectorSearchQuery` and does not embed query text. Query-text embedding exists as a separate application boundary (`DocumentQueryEmbeddingPort` / `DocumentQueryEmbedding`). Query-preparation composition exists as `DocumentVectorSearchQueryPreparationService`. Application query execution composition exists as `RegulatoryIntelligenceQueryExecutionService`. An explicit outer composition root now exists as `build_regulatory_intelligence_query_execution`: it accepts those three application ports and returns a wired `RegulatoryIntelligenceQueryExecutionService`. A provider-aware API composition function now exists as `build_regulatory_intelligence_provider_runtime`: it constructs `OpenAIDocumentQueryEmbeddingAdapter`, `QdrantDocumentVectorSearch`, and `OpenAIRegulatoryConstraintInferenceAdapter` from injected clients, existing `QdrantDocumentVectorConfig`, and two explicit model strings, then delegates to the Chunk 67 builder. Provider clients must still be supplied externally. A configured API composition function now exists as `build_regulatory_intelligence_configured_runtime`: callers can provide those clients plus already-constructed `RegulatoryIntelligenceRuntimeSettings`; the builder maps collection name and vector size into `QdrantDocumentVectorConfig`, forwards the two model fields, and delegates to the Chunk 71 builder. A managed API composition function now exists as `managed_regulatory_intelligence_runtime`: callers can provide already-loaded `OpenAISettings`, `QdrantSettings`, and `RegulatoryIntelligenceRuntimeSettings`; the manager creates clients through existing factories, registers cleanup immediately, delegates to the Chunk 73 builder, yields the query-execution service, and closes both clients. A settings-loaded API composition function now exists as `loaded_regulatory_intelligence_runtime`: callers can supply the existing common `env_file` argument; the loader composition calls the three published settings loaders, delegates to the managed runtime, and yields that same service. A FastAPI-compatible lifespan factory now exists as `build_regulatory_intelligence_lifespan`: constructing it is lazy; entering the returned lifespan callback enters Chunk 75 and exiting it tears Chunk 75 down; the composed service is stored on `app.state.regulatory_intelligence_query_execution_service` only while the lifespan is active and is removed before Chunk 75 teardown. The factory is not a service registry. Production `create_app()` installs it as the default FastAPI lifespan. Active FastAPI requests can resolve that exact service through `get_regulatory_intelligence_query_execution_service`; the accessor does not invoke it. HTTP transport contracts exist. A dedicated HTTP query router uses production-style `Depends(...)` and invokes the query-execution service; production `create_app()` installs that router under the existing API prefix. LangGraph still does not invoke the service. Typed `RegulatoryIntelligenceRuntimeSettings` exists for the two model identifiers plus Regulatory Qdrant collection name and vector size (`ENERGY_REGULATORY_*`) and is loaded by `loaded_regulatory_intelligence_runtime`; it is not loaded by any of those builders, the managed runtime, `create_app()`, or LangGraph. The application agent remains provider-neutral and does not import OpenAI or Qdrant. A concrete infrastructure adapter `OpenAIDocumentQueryEmbeddingAdapter` structurally implements `DocumentQueryEmbeddingPort` with an injected `AsyncOpenAI` client and explicit model. A concrete infrastructure adapter `OpenAIRegulatoryConstraintInferenceAdapter` structurally implements `RegulatoryConstraintInferencePort` with an injected `AsyncOpenAI` client and explicit model; it converts structured provider output into canonical `RegulatoryConstraint` after checking evidence chunk IDs against the supplied normalized chunks. Empty retrieval returns no constraints and does not invoke inference. Existing application exceptions from retrieval or inference propagate unchanged. A dedicated HTTP query router can invoke the query-execution service through `Depends`; production `create_app()` installs that router. LangGraph does not invoke the service.
- **Still deferred:** LangGraph wiring of this agent, the query-execution service, the provider-aware builder, the configured builder, the managed runtime, or the settings-loaded runtime; PDF/OCR; document extraction/indexing pipeline; actual RAG workflow; verified Armenian DAM rule extraction; retry/fallback; prompt catalog. An offline typed OpenAI client-construction foundation exists (`OpenAISettings`, `load_openai_settings`, `create_openai_client`) and is used by the managed runtime; `load_openai_settings` is used by the settings-loaded runtime; it is not used by this agent, either adapter, the Chunk 67/71/73 builders, `create_app()`, or LangGraph. Typed `RegulatoryIntelligenceRuntimeSettings` exists and is received already-constructed by the configured builder and the managed runtime; Chunk 75 loads it through the published loader; it is not loaded by this agent, either adapter, any of those builders, the managed runtime, `create_app()`, or LangGraph. Conceptual failure behavior above is specification, not implemented runtime. The agentt does not construct `RegulatoryConstraint` values itself and does not hardcode market rules.

## 2. Pricing & Sales Agent

- **Kind:** Hybrid — deterministic commercial arithmetic; LLM only for interpreting unstructured commercial terms
- **Purpose:** Translate tariffs, contract positions, and sales commitments into canonical commercial constraints and target volumes/prices for strategy — not unofficial DAM clearing.
- **Canonical inputs:** `RegulatoryConstraint`, contract/tariff canonical records (to be defined when verified), historical `MarketPriceRecord`, optional `LoadForecastPoint`.
- **Canonical outputs:** canonical commercial constraint / pricing intent objects (exact contract TBD when products are verified); never raw spreadsheet rows.
- **External tools/systems (eventual):** structured adapters for contract/tariff files; persistence port; LLM port for narrative clause extraction only.
- **Dependencies:** Regulatory Intelligence Agent outputs; canonical price history. Does not replace DAM Price Forecast Agent.
- **Failure behavior:** Missing tariff tables → DLQ/diagnostics and no silent default prices. LLM extraction failures must not fabricate AMD/MWh (or any currency) figures.

## 3. Weather & Renewable Forecast Agent

- **Kind:** Deterministic service (optional future ML in `ml/`, never LLM-calculated weather)
- **Purpose:** Provide canonical weather and renewable-resource forecasts used as features and operational context.
- **Canonical inputs:** adapter-normalized weather/renewable payloads (never raw vendor schemas); location and horizon identifiers.
- **Canonical outputs:** `WeatherRecord` series.
- **External tools/systems (eventual):** weather/renewable source adapters (HTTP, files, or scrapers — providers TBD), time-series cleaning, persistence.
- **Dependencies:** ACL structured adapters; persistence. Parallel with other Phase 2 agents.
- **Failure behavior:** Source outage → orchestrator fallback (last good forecast, persistence, or skip with degraded-feature flag). Malformed points → DLQ, not interpolated fiction unless a documented cleaning rule applies.
- **Implemented now:** typed application request/result (`WeatherAndRenewableForecastRequest`, `WeatherAndRenewableForecastResult`); concrete application agent (`WeatherAndRenewableForecastAgent`); canonical `WeatherRecord` source port (`WeatherRecordSourcePort`).
- **Still deferred:** external provider; ACL adapter; persistence; n8n; graph wiring; retry/fallback; degraded behavior; ML. Conceptual failure behavior above is specification, not implemented runtime.

## 4. Hydro Resources Agent

- **Kind:** Deterministic service
- **Purpose:** Normalize hydro storage, inflow/outflow, and hydro generation availability into canonical records for risk and bidding.
- **Canonical inputs:** adapter-normalized hydro telemetry or operator files.
- **Canonical outputs:** `HydroRecord` series.
- **External tools/systems (eventual):** structured file/API adapters, persistence, time-series cleaning.
- **Dependencies:** ACL; Generation Availability Agent may consume hydro MW but must not scrape hydro files itself.
- **Failure behavior:** Unnormalizable hydro files → DLQ. Do not assume reservoir operating policy that has not been verified.
- **Implemented now:** typed application request/result (`HydroResourcesRequest`, `HydroResourcesResult`); concrete application agent (`HydroResourcesAgent`); canonical `HydroRecord` source port (`HydroRecordSourcePort`).
- **Still deferred:** external source/provider; ACL adapter; persistence; n8n; graph wiring; retry/fallback execution; degraded behavior; hydrological calculation; Generation Availability coupling. Conceptual failure behavior above is specification, not implemented runtime.

## 5. Generation Availability Agent

- **Kind:** Deterministic service
- **Purpose:** Assemble plant/unit available capacity (planned outages, derates, technology mix) as canonical MW series.
- **Canonical inputs:** `HydroRecord` (optional), adapter-normalized availability/outage records, `RegulatoryConstraint` that affects operable capacity.
- **Canonical outputs:** `GenerationAvailabilityRecord` series (MW).
- **External tools/systems (eventual):** structured adapters for operator/availability files or APIs (TBD), persistence.
- **Dependencies:** Hydro Resources Agent (when hydro is in the portfolio); ACL. Not an LLM.
- **Failure behavior:** Partial fleet coverage is explicit (missing units listed in diagnostics), not silently treated as zero or full availability unless a documented rule says so.
- **Implemented now:** typed application request/result (`GenerationAvailabilityRequest`, `GenerationAvailabilityResult`); concrete application agent (`GenerationAvailabilityAgent`); canonical `GenerationAvailabilityRecord` source port (`GenerationAvailabilityRecordSourcePort`).
- **Still deferred:** external source/provider; ACL adapter; persistence; n8n; graph wiring; retry/fallback execution; degraded behavior; Hydro coupling; fleet completeness policy; capacity derivation; regulatory-capacity application. Conceptual failure behavior above is specification, not implemented runtime.

## 6. News Intelligence Agent

- **Kind:** LLM / reasoning
- **Purpose:** Turn news and public-information items into canonical events that may affect load, outages, or prices — as qualitative context, not as a substitute for ML forecasts.
- **Canonical inputs:** adapter-normalized article/text payloads.
- **Canonical outputs:** `NewsEvent` records with timestamps, relevance, and structured entities when extractable.
- **External tools/systems (eventual):** news/HTML/scraper adapters, LLM provider port, persistence. No named news vendors in this spec.
- **Dependencies:** ACL unstructured/structured adapters; orchestrator. Must not write `PriceForecastPoint` or `LoadForecastPoint`.
- **Failure behavior:** Source or LLM failure → empty event set + diagnostics. Do not hallucinate outage MW or prices. Poison/malformed HTML → DLQ.
- **Implemented now:** typed application request/result (`NewsIntelligenceRequest`, `NewsIntelligenceResult`); concrete application agent (`NewsIntelligenceAgent`); canonical `NewsEvent` source port (`NewsEventSourcePort`).
- **Still deferred:** provider/source acquisition; ACL adapter; scraping/RSS/API; persistence; n8n; graph wiring; retry/fallback; LLM; sentiment/relevance inference; summarization; embedding/vector indexing. Conceptual failure behavior above is specification, not implemented runtime.

## 7. Market Monitoring Agent

- **Kind:** Deterministic service
- **Purpose:** Ingest official or operator-published market observations (historical DAM prices, volumes, status) into canonical time series.
- **Canonical inputs:** adapter-normalized market reports/files/APIs (sources TBD).
- **Canonical outputs:** `MarketPriceRecord` series; later, market-status objects if verified.
- **External tools/systems (eventual):** structured adapters, persistence (TimescaleDB).
- **Dependencies:** ACL. Does not clear the market and does not forecast prices.
- **Failure behavior:** Missing hours / duplicate timestamps handled by time-series cleaning rules or DLQ (`TESTING_STRATEGY.md`). Never silently fill prices.
- **Implemented now:** typed application request/result (`MarketMonitoringRequest`, `MarketMonitoringResult`); concrete application agent (`MarketMonitoringAgent`); canonical `MarketPriceRecord` source port (`MarketPriceRecordSourcePort`). Canonical `EnergyPrice` currency and optional `volume_mwh` pass through unchanged.
- **Still deferred:** official/operator source; ACL adapter; CSV/Excel/API acquisition; persistence; n8n; graph wiring; retry/fallback execution; market-status objects; time-series cleaning execution; gap/duplicate rules; DAM-specific rules; currency conversion; price forecasting; market clearing. Conceptual failure behavior above is specification, not implemented runtime. The agent does not clear the market and does not forecast prices.

## 8. Consumer Load Forecast Agent

- **Kind:** ML-backed
- **Purpose:** Produce consumer load forecasts in MW for the DAM horizon.
- **Canonical inputs:** `ConsumptionRecord` history, `WeatherRecord`, calendar/features as canonical structures, `NewsEvent` / `RegulatoryConstraint` only as **features or flags**, not as LLM prompts that output MW.
- **Canonical outputs:** `LoadForecastPoint` series (MW).
- **External tools/systems (eventual):** `ml/load_forecast` (XGBoost / LightGBM / optional Prophet baseline) behind application ports; feature store/persistence; not an LLM calculator.
- **Dependencies:** Phase 2 canonical series; forecasting ports injected by the composition root. Chief Orchestrator Agent invokes this agent. The agent must not import XGBoost, LightGBM, Prophet, or concrete model classes.
- **Failure behavior:** ML runtime failure → documented fallback (previous model, naive baseline, or abort per orchestrator policy). LLM is not an allowed fallback for the numeric forecast.

## 9. DAM Price Forecast Agent

- **Kind:** ML-backed
- **Purpose:** Produce Day-Ahead price forecasts for each delivery interval.
- **Canonical inputs:** `MarketPriceRecord` history, `LoadForecastPoint`, `GenerationAvailabilityRecord`, `WeatherRecord`, `HydroRecord`, `NewsEvent` features/flags, `RegulatoryConstraint` flags.
- **Canonical outputs:** `PriceForecastPoint` series (`EnergyPrice`, currency explicit).
- **External tools/systems (eventual):** `ml/price_forecast` behind application ports; persistence. Not an LLM calculator.
- **Dependencies:** Load forecast and Phase 2 series as available; forecasting ports injected by the composition root. The agent must not import XGBoost, LightGBM, Prophet, or concrete model classes.
- **Failure behavior:** Same policy as load forecast: no LLM-generated prices. Degraded feature sets must be explicit on the output metadata.

## 10. Portfolio & Risk Agent

- **Kind:** Deterministic / quantitative service
- **Purpose:** Combine positions, forecasts, availability, and constraints into a `RiskAssessment` (limits, exposures, scenarios).
- **Canonical inputs:** `LoadForecastPoint`, `PriceForecastPoint`, `GenerationAvailabilityRecord`, `HydroRecord`, `RegulatoryConstraint`, commercial constraints from Pricing & Sales Agent.
- **Canonical outputs:** `RiskAssessment`.
- **External tools/systems (eventual):** persistence; scenario engines in domain/application. LLM may later explain the assessment; it must not compute VaR or MW exposure.
- **Dependencies:** Forecast agents; regulatory and pricing agents. Not ML training.
- **Failure behavior:** Missing critical forecast → refuse to certify risk as complete; return partial assessment with explicit gaps. No silent “risk OK”.

## 11. Trading Strategy Agent

- **Kind:** Hybrid application agent — deterministic bid construction; optional LLM narrative. **Not** numerical forecasting.
- **Purpose:** Form DAM `MarketBid` objects consistent with risk limits, availability, load, and constraints.
- **Canonical inputs:** `RiskAssessment`, `LoadForecastPoint`, `PriceForecastPoint`, `GenerationAvailabilityRecord`, `RegulatoryConstraint`, commercial constraints.
- **Canonical outputs:** `MarketBid` set.
- **External tools/systems (eventual):** persistence; later a market-gateway adapter (official interface TBD — do not invent a vendor).
- **Dependencies:** Portfolio & Risk Agent; forecast agents; regulatory/pricing outputs.
- **Failure behavior:** If risk is incomplete or constraints conflict, emit no bid (or a documented safe default only if a verified rule exists — none yet). LLM must not invent bid MW or prices.

## 12. Billing & Settlement Agent

- **Kind:** Deterministic service
- **Purpose:** Compare bids, `MarketClearingResult`, and metered/canonical energy into `SettlementResult`.
- **Canonical inputs:** `MarketBid`, `MarketClearingResult`, `ConsumptionRecord` / delivered energy canonicals, fee/tariff canonicals when verified.
- **Canonical outputs:** `SettlementResult`.
- **External tools/systems (eventual):** structured adapters for official settlement reports, persistence. Not LLM arithmetic.
- **Dependencies:** Clearing results (ingested via ACL); strategy bids. Official settlement rules TBD.
- **Failure behavior:** Unmatched intervals or unverifiable prices → DLQ/diagnostics, never a silently rounded invoice.

## 13. Chief Orchestrator Agent

- **Kind:** Orchestration (LangGraph, planned)
- **Purpose:** Own graph state, phase routing, parallel Phase 2 join, retries, fallback, diagnostics, and workflow status from contract phase through settlement.
- **Canonical inputs:** workflow command (delivery date, portfolio ID, correlation ID); all agent outputs as canonical state slices.
- **Canonical outputs:** workflow status, aggregated diagnostics, final canonical artifacts (`MarketBid`, `SettlementResult`, etc. as the phase produces them).
- **External tools/systems (eventual):** LangGraph runtime, Redis ephemeral state, persistence for workflow records, logging/observability ports.
- **Dependencies:** All other agents via ports/use cases. Must not call concrete adapters or ML trainers directly; it calls agents/use cases. LangGraph nodes must depend on application abstractions, not concrete infrastructure or ML packages.
- **Failure behavior:** Transient agent/I/O failures retry per policy. Unrecoverable ingestion stays in DLQ; graph continues with degraded flags or stops according to explicit phase policy. Never swallow exceptions. Never ask an LLM to replace ML or settlement math.
- **Available foundation (not a concrete orchestrator):** `AgentPort`; `WorkflowState`; LangGraph runtime that injects `ParallelIngestionWorkflowStep` and `ParallelIngestionFailureRuntimeHandlingService` and applies `advance_after_parallel_ingestion` (`START → workflow_entry → parallel_ingestion`, then `ingestion`/`running` → `parallel_ingestion_success_transition` → `END` or `ingestion`/`failed` → `END`); failure-policy hook (`FailurePolicyPort.decide` → `RETRY` | `FALLBACK` | `FAIL`); typed parallel-ingestion fan-out plan (`ParallelIngestionPlan` with the five existing Phase 2 request DTOs); typed parallel-ingestion execution boundary (`ParallelIngestionExecutionPort.execute(plan) → ParallelIngestionSuccess`); typed all-five-success fan-in aggregate (`ParallelIngestionSuccess` with the five existing Phase 2 result DTOs); concrete LangGraph-free `ConcurrentParallelIngestionExecutor` that concurrently invokes the five current ingestion agents on the all-success path; typed workflow-context boundary (`ParallelIngestionWorkflowContextPort.resolve_plan` / `record_success`) that keeps Phase 2 payloads outside `WorkflowState`; process-local in-memory `InMemoryParallelIngestionWorkflowContext` as local/dev reference infrastructure; framework-neutral `ParallelIngestionWorkflowStep` that composes resolve → execute → record and returns the original `WorkflowState` unchanged; framework-neutral successful Phase 2 control-state transition (`advance_after_parallel_ingestion`) that maps `ingestion`/`running` to `forecasting`/`running`; framework-neutral terminal Phase 2 failure transition (`fail_parallel_ingestion`) that maps `ingestion`/`running` to `ingestion`/`failed` without mutating diagnostics; Phase-2-specific failure-policy decision boundary (`ParallelIngestionFailureDecisionService.decide(context) → FailureAction`) that delegates an already-constructed `FailurePolicyContext` to `FailurePolicyPort` exactly once and returns the published action unchanged without executing it; Phase-2-specific failure-policy context constructor (`build_parallel_ingestion_failure_policy_context`) that builds the published `FailurePolicyContext` from already-sanitized typed facts without inspecting exceptions or inventing an agent identity; Phase-2-specific terminal `FAIL` action execution (`execute_parallel_ingestion_failure_action`) that delegates `FailureAction.FAIL` to `fail_parallel_ingestion` and rejects `RETRY` / `FALLBACK` as not implemented; Phase-2-specific prepared failure-handling composition (`ParallelIngestionFailureHandlingService.handle(state, context) → WorkflowState`) that decides then executes without constructing context or branching on the action; Phase-2-specific agent-failure attribution (`ParallelIngestionAgentFailure`) that preserves canonical `AgentName` at the concurrent executor boundary through exception chaining; Phase-2-specific ExceptionGroup attributed-leaf extraction (`extract_parallel_ingestion_agent_failures`) that returns already-attributed leaves in depth-first encounter order without classifying them, Phase-2-specific sanitized one-leaf failure classification (`classify_parallel_ingestion_agent_failure`) that produces frozen `ParallelIngestionFailureFact` (`AgentName` + `error_code`) without selecting among failures, Phase-2-specific tuple-level failure-fact classification (`classify_parallel_ingestion_agent_failures`) that maps `tuple[ParallelIngestionAgentFailure, ...]` to `tuple[ParallelIngestionFailureFact, ...]` by delegating each element to that one-leaf classifier without selecting among failures, and a Phase-2-specific failure-fact selection contract (`ParallelIngestionFailureSelectionPort.select(facts) -> ParallelIngestionFailureFact`) that defines the selection seam, and a Phase-2-specific strict single-failure selector (`StrictSingleParallelIngestionFailureSelector.select(facts) -> ParallelIngestionFailureFact`) that returns the unique sanitized fact unchanged or fails closed on zero or multiple facts without ranking or priority semantics, and a Phase-2-specific attempt-number source contract (`ParallelIngestionAttemptNumberPort.get_attempt_number(workflow_id) -> int`) that defines where the current 1-based attempt is read without increment/reset semantics on the port, and a Phase-2-specific initial attempt-number source (`InitialParallelIngestionAttemptNumberSource.get_attempt_number(workflow_id) -> int`) that returns exactly `1` for the current no-retry runtime without tracking or persistence, and a Phase-2-specific failure-policy context resolution service (`ParallelIngestionFailureContextResolutionService.resolve(workflow_id, phase, facts) -> FailurePolicyContext`) that selects once, awaits attempt lookup once, and delegates to the published context builder without implementing a selector, tracker, policy, or action, and a Phase-2-specific failure-context preparation service (`ParallelIngestionFailureContextPreparationService.prepare(workflow_id, phase, failure_group) -> FailurePolicyContext`) that delegates extraction, tuple classification, and context resolution without invoking policy or executing an action, and a Phase-2-specific initial terminal-fail failure policy (`InitialParallelIngestionFailurePolicy.decide(context) -> FailureAction`) that structurally satisfies `FailurePolicyPort` and returns exactly `FailureAction.FAIL` for every valid context without inspecting fields or executing the action, and a Phase-2-specific runtime failure-handling composition (`ParallelIngestionFailureRuntimeHandlingService.handle(state, failure_group) -> WorkflowState`) that composes failure-context preparation with existing failure handling from a `WorkflowState` plus `BaseExceptionGroup` without inspecting exceptions, selecting failures, resolving attempts, or deciding actions directly.
- **Still absent:** concrete Chief Orchestrator; five-phase routing; parallel graph join for failure/degraded/runtime composition; production/durable workflow-context implementation; Phase 3 routing/execution; general status lifecycle transitions; partial/degraded fan-in policy; concrete multi-failure selector; simultaneous failure resolution; retry-capable attempt tracking; increment/reset semantics; retry execution; fallback execution; LangGraph wiring of `fail_parallel_ingestion`; LangGraph wiring of `ParallelIngestionFailureDecisionService`; LangGraph wiring of `build_parallel_ingestion_failure_policy_context`; LangGraph wiring of `ParallelIngestionFailureContextResolutionService`; LangGraph wiring of `execute_parallel_ingestion_failure_action`; LangGraph wiring of `ParallelIngestionFailureHandlingService`; LangGraph wiring of `extract_parallel_ingestion_agent_failures`; LangGraph wiring of `classify_parallel_ingestion_agent_failure`; LangGraph wiring of `classify_parallel_ingestion_agent_failures`; LangGraph wiring of `ParallelIngestionFailureSelectionPort`; LangGraph wiring of `StrictSingleParallelIngestionFailureSelector`; LangGraph wiring of `ParallelIngestionAttemptNumberPort`; LangGraph wiring of `InitialParallelIngestionAttemptNumberSource`; LangGraph wiring of `ParallelIngestionFailureContextPreparationService`; LangGraph wiring of `InitialParallelIngestionFailurePolicy`; runtime policy/action composition inside the graph factory; retry/fallback-capable failure policy; exception-to-diagnostic mapping; Regulatory Intelligence graph wiring; `create_app()` wiring of `build_regulatory_intelligence_query_execution`, `build_regulatory_intelligence_provider_runtime`, `build_regulatory_intelligence_configured_runtime`, `managed_regulatory_intelligence_runtime`, or `loaded_regulatory_intelligence_runtime`; LangGraph wiring of `RegulatoryIntelligenceQueryExecutionService`; verified Armenian DAM rule extraction. Conceptual failure behavior above is specification, not implemented runtime. The graph invokes the Phase 2 workflow step, catches only `BaseExceptionGroup` for the injected runtime handling composition, and then applies the published success transition on the `ingestion`/`running` route; it does not call `AgentPort` or the five Phase 2 agents directly, does not call `RegulatoryIntelligenceAgent`, does not execute Phase 3, does not execute the terminal failure transition directly, and does not route failures through the decision service, context builder, context-resolution service, failure-context preparation service, initial failure policy, action executor, prepared handling composition, ExceptionGroup extractor, sanitized-fact classifier, tuple-level classifier, failure-fact selection port, strict single-failure selector, attempt-number source port, or initial attempt-number source. Agent failure attribution exists at the Phase 2 executor boundary; ExceptionGroup attributed-leaf extraction now exists; sanitized one-leaf classification now exists; tuple-level sanitized classification now exists; a Phase-2-specific selection contract now exists; a Phase-2-specific attempt-number source contract now exists; a Phase-2-specific failure-context resolution service now exists; a strict single-failure selector now exists for the unambiguous one-fact case; simultaneous multi-failure selection remains unresolved; there is no first/last/priority winner rule; an initial attempt-number source now exists that always returns `1`; there is no retry-capable attempt tracker and no increment/reset semantics; a Phase-2-specific failure-context preparation service now exists that composes extraction → classification → context resolution into `FailurePolicyContext`; the preparation service is callable only when a constructed resolution service is injected; the strict selector and initial attempt source exist and can be injected behind that resolution service, but are not auto-wired; an initial terminal-fail failure policy now exists that always returns `FailureAction.FAIL` and is not auto-wired into preparation, handling, or LangGraph; a Phase-2-specific runtime failure-handling composition now exists that composes preparation plus existing handling from `WorkflowState` plus `BaseExceptionGroup` and is injected into LangGraph at the Phase 2 step boundary; retry-capable attempt tracking does not; simultaneous multi-failure resolution and attempt tracking still do not. Direct non-group Phase 2 exceptions still propagate. The in-memory context adapter is not composed by a Chief Orchestrator and is not wired into the graph or API.

---

## Phase mapping (reference)

| Business phase | Agents |
| --- | --- |
| 1. Contract & regulatory alignment | Regulatory Intelligence Agent, Pricing & Sales Agent |
| 2. Parallel ingestion | Weather & Renewable Forecast Agent, Hydro Resources Agent, Generation Availability Agent, News Intelligence Agent, Market Monitoring Agent |
| 3. Forecasting | Consumer Load Forecast Agent, DAM Price Forecast Agent |
| 4. Portfolio, risk & strategy | Portfolio & Risk Agent, Trading Strategy Agent |
| 5. Clearing, billing & settlement | Billing & Settlement Agent |
| All phases | Chief Orchestrator Agent |
