# Testing Strategy

Tests protect the Anti-Corruption Layer, canonical contracts, dependency direction, and later ML quality.

Default rule: tests **must not** depend on live external APIs. Use `tests/fixtures/` and fakes/stubs behind ports.

## Current toolchain (Chunk 1)

All commands are run through uv so a separately activated virtualenv is not required:

| Tool | Role | Command |
| --- | --- | --- |
| pytest | Test runner | `uv run pytest` |
| pytest-asyncio | Async test support | enabled via `asyncio_mode = auto` |
| HTTPX | ASGI transport for API tests (no real TCP server) | used in `tests/unit/test_health.py` and `tests/unit/api/` |
| Ruff | Lint (pycodestyle, pyflakes, import order, bugbear, pyupgrade) and format | `uv run ruff check .` / `uv run ruff format --check .` |
| mypy | Strict typing of `src` | `uv run mypy src` |

Settings tests must not depend on a developer’s local `.env`. Construct `AppSettings` / `load_settings(env_file=None)` and `DatabaseSettings` / `load_database_settings(env_file=None)` and call `clear_settings_cache()` between cases. Health and API tests inject settings through `create_app(settings=...)`. Database settings tests also clear `ENERGY_DB_*` environment variables.

Domain contract tests live in `tests/unit/domain/` and do not use FastAPI. They cover UTC timestamp normalization, frozen/extra-forbid behavior, MW/MWh constraints, Decimal money (including float rejection), generation/regulatory/bid/settlement/DLQ invariants, and JSON serialization.

Chunk 3 observability and error-boundary tests:

- Application error mapping (`tests/unit/api/test_error_mapping.py`): `InvalidRequestError`→400, `ResourceNotFoundError`→404, `ConflictError`→409, `DependencyUnavailableError`→503, plus unknown-route 404. Uses test-only routes, not production endpoints.
- Unmapped application errors fail closed: bare `ApplicationError` and an unregistered subclass both become sanitized HTTP 500 `internal_error`; original messages/codes are not exposed.
- Request validation sanitization: HTTP 422 `request_validation_error` envelope; correlation ID present; field details included; raw invalid `input` omitted.
- Unexpected exception sanitization: HTTP 500 `internal_error` with a generic public message; secret-looking runtime text, exception class, traceback, and module path are absent from the client body; internal structured logs retain the correlation ID and exception.
- Unexpected 500 request-completion observability: the same request emits one `http_request_completed` event with `status_code=500` and the same correlation ID as the sanitized 500 response, distinct from the internal exception log.
- Correlation propagation on `GET /api/v1/health`: valid `X-Correlation-ID` reused; missing ID generated; invalid ID replaced without rejecting the request.
- Correlation isolation: sequential requests keep independent IDs; concurrent async requests with distinct supplied IDs do not contaminate each other.
- Structured logger formatting (`tests/unit/observability/test_logging.py`): JSON records include UTC timestamp, level, logger, message, and correlation ID (or JSON `null` when unbound). Unknown extras are dropped.
- Idempotent logging setup: repeated `configure_logging` / `create_app` calls do not stack duplicate handlers and do not reconfigure pytest's root capture.
- Application-layer dependency enforcement (`tests/architecture/test_application_dependencies.py`): `energy_trading.application` must not import `energy_trading.api`, `infrastructure`, `ml`, FastAPI, or Starlette.

Chunk 4 structured-ingestion boundary tests:

- Structural Protocol implementation with a test-only `FakeConsumptionAdapter` (`tests/unit/application/`): the fake does not inherit an infrastructure base class; `async ingest()` returns `StructuredIngestionResult[ConsumptionRecord]`.
- Canonical success, partial success, complete normalization failure, and valid empty source. Partial success keeps the canonical record and isolates the failure as `DLQRecord.payload_reference`.
- Immutable batch collections: `StructuredIngestionResult` is a frozen dataclass; `records`, `diagnostics`, and `dlq_records` are tuples.
- No raw payload is embedded in the result envelope.
- Test-only in-memory `DeadLetterQueuePort` fake accepts canonical `DLQRecord` without Redis or other infrastructure.
- ACL/port architecture tests (`tests/architecture/test_structured_ingestion_boundary.py`): AST inspection of the ingestion-port modules for forbidden raw-source imports and annotations (`pandas`, `openpyxl`, `csv`, `requests`, `httpx`, `aiohttp`, BeautifulSoup, `bytes`, `bytearray`, `dict`, `Mapping`, `Any`, `DataFrame`). Application ports must not import `energy_trading.infrastructure`, `energy_trading.api`, `energy_trading.ml`, FastAPI, or Starlette.

`tests/architecture/test_domain_dependencies.py` uses the standard library `ast` module to fail if `energy_trading.domain` imports `energy_trading.api`, `application`, `infrastructure`, `ml`, `shared`, FastAPI, SQLAlchemy, psycopg, or Alembic. No extra architecture-testing dependency is used.

Infrastructure integration tests against a running PostgreSQL process are **not** run by default. They require the Compose `postgres` profile and `ENERGY_RUN_POSTGRES_INTEGRATION=1`. Chunk 13 added offline PostgreSQL foundation tests that require no database service. Chunk 14 added offline Consumption table, migration, and repository tests that also require no database service. Chunk 17 added offline Redis settings, client-factory, codec, and `RedisCache` tests that require no Redis process. Chunk 18 added opt-in live Redis tests (`redis_integration`, `ENERGY_RUN_REDIS_INTEGRATION=1`) that require the Compose `redis` profile. Chunk 24 added opt-in live Qdrant tests (`qdrant_integration`, `ENERGY_RUN_QDRANT_INTEGRATION=1`) that require the Compose `qdrant` profile. Chunk 25 added opt-in live n8n tests (`n8n_integration`, `ENERGY_RUN_N8N_INTEGRATION=1`) that require the Compose `n8n` profile.

## Layout

| Directory | Intent |
| --- | --- |
| `tests/unit/` | Domain contracts/value objects, settings, health, application errors, API envelope, observability, structured-ingestion ports, document-extraction ports, document-embedding ports, document-vector-index ports, cache port, Redis settings/client/cache adapter, infrastructure adapters, filesystem DLQ persistence, PostgreSQL engine/Alembic foundation |
| `tests/integration/` | Opt-in live PostgreSQL/TimescaleDB tests (`postgres_integration`), opt-in live Redis cache tests (`redis_integration`), opt-in live Qdrant vector tests (`qdrant_integration`), and opt-in live n8n readiness tests (`n8n_integration`) |
| `tests/architecture/` | Import-graph / layering rules |
| `tests/fixtures/` | CSV/Excel/PDF snippets, malformed series, canonical JSON |

## Planned suites

### Domain unit tests

Implemented in Chunk 2 (`tests/unit/domain/`): value objects, UTC vs naive timestamps, unknown-field / frozen-model behavior, non-negative MW, positive bid quantity, NaN/Infinity rejection, currency format, Decimal money, generation available≤total, regulatory date/bound windows, settlement period and single-currency, DLQ non-empty diagnostics, Pydantic JSON serialization.

Later: MW vs MWh consistency when a verified interval exists; incomplete `RiskAssessment` completeness flags when that field is added.

### Adapter normalization tests

Happy-path CSV/Excel/API-shaped fixtures → canonical models. Armenian headers and renamed columns map through semantic mapping, not through domain if-else on vendor names.

### Malformed-data tests

Truncated files, wrong types, empty sheets, HTML when CSV was expected. Expect validation errors and **DLQ**, not process crash.

### Unit-conversion tests

Chunk 8 covers Consumption MW identity and explicit kW→MW (`1000 kW = 1 MW`, `12500 kW = 12.5 MW`) in `tests/unit/infrastructure/adapters/structured/normalization/`. Boolean measurements are rejected. Energy units (MWh/kWh) are not power and are not converted. Header/config mismatches fail closed. Later domains may add kWh→MWh, °F→°C, knots→m/s; unknown units still fail closed (DLQ), not guessed.

### Timezone tests

Chunk 8 covers explicit IANA localization (`zoneinfo` + `tzdata`) with `Europe/Berlin` DST fixtures: aware instants keep UTC; naive values fail without a configured zone; naive values with an explicit zone localize; ambiguous and nonexistent local clocks fail closed. No timezone is inferred, including no default `Asia/Yerevan`. Output remains timezone-aware canonical UTC.

### Missing-hour tests

Chunk 10 covers internal missing intervals on an explicit `IntervalGrid` only. Gaps are compact ranges, not expanded timestamp lists. Leading/trailing delivery-window completeness remains a later coverage-window policy.

### Duplicate-timestamp tests

Chunk 9 covers Consumption duplicate interval keys: every member of a `(consumer_id, canonical UTC timestamp)` group is rejected. There is no silent first/last/average winner. Tests name that fail-closed rule.

### DLQ tests

Unrecoverable records produce `DLQRecord` with stage, source, correlation ID, and payload ref. Workflow continues for siblings. No silent swallow.

Chunk 4 covers the application sink Protocol with an in-memory fake (`enqueue(DLQRecord)`). Chunk 11 covers the interim filesystem metadata implementation (`tests/unit/infrastructure/persistence/test_dlq.py`): one JSON artifact per canonical record; directory creation; round-trip into `DLQRecord`; canonical keys only; diagnostic and optional `correlation_id` survival; identical retry idempotency without a second file; same-ID/different-metadata `ConflictError` without overwrite; hashed filenames that cannot escape the configured root; sanitized `DependencyUnavailableError` for create/write/corrupt-file failures; and async `enqueue()` offload via `asyncio.to_thread`. Tests use `tmp_path` only. PostgreSQL integration tests and DLQ replay/list/delete tests remain later chunks.

### Agent tests

Each agent tested with port fakes. Assert canonical in/out. Five concrete agents exist: Weather & Renewable Forecast Agent, Hydro Resources Agent, Generation Availability Agent, News Intelligence Agent, and Market Monitoring Agent; remaining agents stay fake-driven until implemented. Especially: Consumer Load Forecast Agent and DAM Price Forecast Agent call forecasting **ports**, **not** an LLM port and **not** concrete ML libraries, for numeric outputs.

### LangGraph routing tests

Chunk 28 covers the original no-op skeleton, not business routing. Planned later: contract → parallel ingestion join → forecast → risk → strategy → clearing → settlement. Retry/fallback *execution*, degraded flags, and five-phase routing tests do not exist yet. Chunk 29 covers the policy *decision* hook only; it does not execute retries or fallbacks. Chunk 35 covers the typed parallel-ingestion *fan-out plan* only. Chunk 36 covers the typed all-five-success *fan-in aggregate* only. Chunk 37 covers the typed *execution boundary* Protocol only. Chunk 38 covers the first concrete executor: five real application-agent invocations through source-port fakes, deterministic concurrency via start events plus a release gate, all-success aggregation with result-identity preservation, native TaskGroup failure/cancellation without retries, and architecture isolation. Chunk 39 covers the typed workflow-context Protocol only: a test-only structural fake, exact async `resolve_plan` / `record_success` signatures, workflow-identity preservation, plan/success identity preservation, and architecture isolation with `WorkflowState` non-expansion. Chunk 40 covers the framework-neutral workflow step: exact resolve → execute → record ordering, workflow-id and plan/success identity preservation, original `WorkflowState` identity/non-mutation, and short-circuit failure propagation without retry. Chunk 41 covers dependency-injected LangGraph invocation of that step on the all-success path: topology `START → workflow_entry → parallel_ingestion → END`, one successful Phase 2 delegation, semantic `WorkflowState` preservation, and failure propagation without retry. Chunk 42 covers the LangGraph-free successful Phase 2 control-state transition: valid `ingestion`/`running` → new `forecasting`/`running` snapshot, input non-mutation, exact identity/date/diagnostic preservation, parameterized wrong-phase and wrong-status `InvalidRequestError` rejection, deterministic equivalent outputs, and architecture isolation. Chunk 43 covers LangGraph wiring of that published function: topology `START → workflow_entry → parallel_ingestion → parallel_ingestion_success_transition → END` on the success path, success ending `forecasting`/`running` while the step still sees `ingestion`/`running`, deterministic `astream` node order, step-failure short-circuit without transition execution or retry. Chunk 62 extends the same file with Phase 2 `BaseExceptionGroup` capture, injected runtime-handler delegation, `ingestion`/`failed` routing to `END`, non-group exception regression, and fail-closed unexpected post-Phase-2 state routing. Chunk 44 covers the process-local in-memory context adapter: constructor-copied prepared plans, sanitized missing-plan `ResourceNotFoundError`, first-write success, value-equal idempotent retry, conflict on differing success, independent workflow identities, concurrent equal retries via `asyncio.TaskGroup`, structural Protocol assignment, and architecture isolation. Chunk 45 covers the LangGraph-free terminal Phase 2 failure transition: valid `ingestion`/`running` → new `ingestion`/`failed` snapshot, input non-mutation, exact identity/date/diagnostic preservation, parameterized wrong-phase and wrong-status `InvalidRequestError` rejection including already-failed states, deterministic equivalent outputs, success/failure policy separation (`FORECASTING`/`RUNNING` vs `INGESTION`/`FAILED`), architecture isolation, and graph/failure-policy non-wiring. Chunk 46 covers the Phase-2-specific failure-policy decision boundary: each published `FailureAction` (`RETRY`, `FALLBACK`, `FAIL`) passes through unchanged, the exact supplied `FailurePolicyContext` object is delegated, the injected `FailurePolicyPort.decide` is awaited exactly once, existing application exceptions from the policy propagate with no default action, structural fakes need not inherit the Protocol, architecture isolation from LangGraph/infrastructure/API/ML/agents/plan/success/executor/context/workflow-step/terminal-failure-transition, and graph/workflow non-wiring. Chunk 47 covers the Phase-2-specific failure-policy context constructor: exact published-field construction from keyword-only typed facts, deterministic value-equivalent outputs, preservation of published `FailurePolicyContext` validation, signature checks that required facts have no speculative defaults while `agent_name` keeps the published optional `None`, architecture proof of no raw exception/traceback handling, no fabricated agent identity, and graph/workflow/decision/transition non-wiring. Chunk 48 covers the Phase-2-specific terminal `FAIL` action executor: `FAIL` delegates to `fail_parallel_ingestion` with a new `ingestion`/`failed` snapshot, input immutability, identity/date/correlation/diagnostic preservation, value-equivalence to the published transition, invalid-state `InvalidRequestError` propagation without remapping, `RETRY` and `FALLBACK` as existing `InvalidRequestError` with exact sanitized not-implemented messages and no state mutation, exhaustive coverage of current `FailureAction` members, architecture isolation, and graph/workflow/decision/context/transition non-wiring. Chunk 49 covers the Phase-2-specific prepared failure-handling composition: an already-built `FailurePolicyContext` plus `WorkflowState` is decided exactly once then executed through the published action executor, `FAIL` preserves terminal-transition behavior, `RETRY` and `FALLBACK` propagate the published not-implemented errors, policy exceptions propagate without reaching the executor, invalid-state `FAIL` preserves published transition validation, architecture isolation, and graph/API non-wiring. Chunk 50 covers Phase 2 agent-failure attribution at the concurrent executor: canonical `AgentName` on `ParallelIngestionAgentFailure`, sanitized wrapper messages, cause chaining, sibling cancellation not reclassified as agent failure, independent attribution when more than one task actually fails, all-success regression, architecture isolation, and graph/policy non-wiring. Chunk 51 covers ExceptionGroup attributed-leaf extraction: single and multiple attributed leaves returned by identity, nested depth-first left-to-right order, same-agent duplicates retained, `__cause__` identity preserved, unattributed leaves including nested and `CancelledError` fail closed as sanitized `InvalidRequestError` without partial results, `BaseExceptionGroup` traversal, architecture isolation, and graph/executor/policy non-wiring. Chunk 52 covers sanitized one-leaf failure classification: frozen `ParallelIngestionFailureFact` with exactly `agent_name` and `error_code`, `ApplicationError` causes reuse their published application error code, unexpected and missing causes collapse to `parallel_ingestion_unexpected_failure` without leaking exception text or class names, all five Phase 2 agent identities pass through unchanged, causal `__cause__` identity is preserved, architecture isolation, and graph/extractor/policy non-wiring. Chunk 53 covers tuple-level failure-fact classification: empty tuple remains empty, one/many attributed failures are classified by delegating to the existing one-leaf classifier, encounter order and duplicate agent identities are preserved without collapsing, mixed existing error-code and unexpected/missing-cause semantics pass through unchanged, original attribution objects are not mutated, equivalent inputs are deterministic, no primary-failure selection is introduced, architecture isolation, and graph/extractor/policy non-wiring. Chunk 54 covers the Phase-2-specific failure-fact selection contract: a test-only structural fake satisfies synchronous `ParallelIngestionFailureSelectionPort.select` without inheriting the Protocol, exact tuple-in / one-fact-out annotations, exact tuple and fact identity preservation, duplicate agent identities and mixed error codes remain representable, no exception/state/attempt/context/action dependency, no concrete winner semantics, architecture isolation, and graph/API non-wiring. Chunk 55 covers the Phase-2-specific attempt-number source contract: a test-only structural fake satisfies async `ParallelIngestionAttemptNumberPort.get_attempt_number` without inheriting the Protocol, exact `workflow_id: str` / `int` annotations, exact workflow-ID identity preservation, valid 1-based values such as `1` and `3` remain representable without production transformation, no state/failure/policy/exception inputs, no write operations, no storage implementation, architecture isolation, and graph/state/API non-wiring. Chunk 56 covers the Phase-2-specific failure-policy context resolution service: structural selector and attempt-number fakes, exact-once `select` then exact-once `get_attempt_number`, selected-fact and workflow-ID identity preservation, published-builder context construction, selector failure short-circuit without attempt lookup, attempt-source failure short-circuit without context construction, existing builder validation propagation, no policy/action/graph coupling, architecture isolation, and graph/API non-wiring. Chunk 57 covers the Phase-2-specific strict single-failure selector: structural conformance to the published selection port without Protocol inheritance, exact-one identity preservation, empty and multi-fact sanitized `InvalidRequestError`, duplicate/equal facts rejected without deduplication, mixed agents/error codes rejected without ranking, Chunk 56 service injection with a test-only attempt source, attempt source not called after multi-failure selection failure, architecture isolation, and unchanged graph/state. Chunk 58 covers the Phase-2-specific initial attempt-number source: structural conformance to the published attempt-number port without Protocol inheritance, async `get_attempt_number`, exact literal `1` for multiple workflow IDs, deterministic repeated calls, no per-workflow mutation, real composition with the strict selector and context-resolution service producing `FailurePolicyContext.attempt_number == 1`, multi-fact fail-closed at the selector, architecture isolation, and unchanged graph/state. None of Chunks 35–58 executes retry/fallback *mechanics*, a general multi-failure selection policy, retry-capable attempt tracking, degraded continuation, production/durable context storage, Phase 3 execution, or orchestrator join. Those tests remain planned. Nodes stay thin (no business formulas in node bodies — architecture assertion as feasible). Nodes depend on application abstractions only.

### Infrastructure integration tests

Opt-in (marker) tests for PostgreSQL/TimescaleDB, Redis, Qdrant, and n8n against Compose **when** those services exist. Not run by default. Redis live tests require the Compose `redis` profile and `ENERGY_RUN_REDIS_INTEGRATION=1`. Qdrant live tests require the Compose `qdrant` profile and `ENERGY_RUN_QDRANT_INTEGRATION=1`. n8n live tests require the Compose `n8n` profile and `ENERGY_RUN_N8N_INTEGRATION=1`. Still no live third-party market APIs.

### API tests

`GET /api/v1/health` is covered with HTTPX ASGI transport. Error envelope, sanitized validation/500 responses, correlation header propagation, and correlation isolation (including a concurrent async case) are covered in Chunk 3. Routers must not contain business rules (behavior + architecture).

### ML validation / backtesting

Walk-forward or holdout on **versioned local datasets**. Metrics recorded in `EXPERIMENT_LOG.md`. Not a substitute for unit tests. No requirement to hit external weather/market APIs during CI.

### Architecture / dependency tests

Implemented for domain: fail if `src/energy_trading/domain/` imports `energy_trading.api`, `energy_trading.application`, `energy_trading.infrastructure`, `energy_trading.ml`, `energy_trading.shared`, FastAPI, SQLAlchemy, psycopg, or Alembic.

Implemented for application (Chunk 3, extended in Chunk 13): fail if `src/energy_trading/application/` imports `energy_trading.api`, `energy_trading.infrastructure`, `energy_trading.ml`, FastAPI, Starlette, SQLAlchemy, psycopg, or Alembic.

Implemented for the structured ingestion ACL boundary (Chunk 4): fail if application ingestion ports import raw-source libraries or annotate `ingest`/`enqueue` with `bytes`, `dict`, `Mapping`, `Any`, `DataFrame`, or similar escape hatches. Application ports may import canonical domain contracts. Infrastructure may later import application ports.

Chunk 6 Consumption CSV adapter tests (`tests/unit/infrastructure/adapters/structured/csv/`):

- tmp_path fixtures only; no live services. Stdlib CSV files, including UTF-8 Armenian identifiers and a test-only Armenian header alias.
- Exact canonical headers and the MW-safe `Consumption_MW` alias produce `ConsumptionRecord` values in source order with UTC timestamps and no DLQ.
- `Consumpton_MW` is accepted as fuzzy MW mapping with a warning whose `field_name` is canonical `value_mw`; the raw typo does not appear in outward diagnostics.
- Default profile rejects unit-ambiguous or energy-like headers (`Consumption_kW`, `Consumption_MWh`, `Consumption_MW_h`, and similar) as canonical MW (schema fail-closed unless `PowerUnit.KW` is explicit).
- Naive timestamps, negative/nonnumeric/NaN/Inf MW, and row-width mismatches isolate the bad row (diagnostic + DLQ) and keep siblings.
- Bare numeric timestamp strings (for example `1710000000`) fail closed and are not treated as Unix timestamps.
- Partial success, complete row-normalization failure, empty file, and header-only success.
- Fatal schema for missing required fields, header collisions, and forced ambiguity; unresolved extra columns warn and continue.
- Missing/unreadable files raise sanitized `DependencyUnavailableError` without the filesystem path. Malformed CSV and invalid UTF-8 are normalization failures with source-level DLQ metadata, not dependency errors.
- A unique raw bad cell value must not appear in diagnostics, DLQ metadata (except opaque source/row references), or serialized result fields.
- Architecture test (`tests/architecture/test_csv_adapter_boundary.py`): CSV adapter must not import pandas/Polars/openpyxl, HTTP clients, FastAPI, databases, LangChain/LangGraph/OpenAI, or ML libraries. Application ports still have no CSV/path/raw-row surface.

Chunk 7 Consumption Excel adapter tests (`tests/unit/infrastructure/adapters/structured/excel/`):

- Temporary `.xlsx` workbooks generated in `tmp_path`; no external Excel files and no live services.
- Exact canonical headers and the MW-safe `Consumption_MW` alias produce `ConsumptionRecord` values in worksheet order with UTC timestamps and no DLQ.
- `Consumpton_MW` is accepted as fuzzy MW mapping with a warning whose `field_name` is canonical `value_mw`; the raw typo does not appear in outward diagnostics.
- Default profile rejects unit-ambiguous or energy-like headers (`Consumption_kW`, `Consumption_MWh`, `Consumption_MW_h`, and similar) as canonical MW (schema fail-closed unless `PowerUnit.KW` is explicit).
- Timezone-aware ISO timestamp strings normalize to UTC. Excel typed naive datetime cells fail the row without timezone inference; sibling valid rows survive.
- Numeric timestamp cells (integers/floats such as Unix-like or Excel-serial numbers) and boolean `value_mw` cells fail closed and are not coerced; sibling valid rows survive.
- Negative/nonnumeric/NaN/Inf MW and missing required cells isolate the bad row (diagnostic + DLQ) and keep siblings.
- Partial success, empty worksheet, and header-only success.
- Fatal schema for missing required fields, header collisions, and forced ambiguity; unresolved extra columns warn and continue.
- Explicit `sheet_name` selects that worksheet. A missing configured worksheet is a sanitized source/schema failure with DLQ metadata, not a silent active-sheet fallback and not a leaked worksheet name.
- Missing/unreadable files raise sanitized `DependencyUnavailableError` without the filesystem path. Corrupt `.xlsx` bytes are normalization failures with source-level DLQ metadata, not dependency errors.
- A unique raw bad cell value must not appear in diagnostics, DLQ metadata (except opaque source/row references), or serialized result fields.
- Shared Consumption mapping tests (`tests/unit/infrastructure/adapters/structured/test_consumption_mapping.py`) prove CSV and Excel reuse the same MW-safety policy; existing CSV adapter tests remain the CSV regression suite.
- Architecture test (`tests/architecture/test_excel_adapter_boundary.py`): Excel adapter may import openpyxl and the shared mapping helper, but must not import pandas/Polars/xlrd, HTTP clients, FastAPI, databases, LangChain/LangGraph/OpenAI, or ML libraries. Application ports still have no Workbook/Worksheet/Cell/path surface.

Chunk 8 Consumption unit/timezone normalization tests:

- Primitive tests (`tests/unit/infrastructure/adapters/structured/normalization/`): MW identity; `1000 kW → 1 MW` and `12500 kW → 12.5 MW`; numeric strings; boolean rejection; deterministic repeated conversion; no energy-unit enum. Aware datetime/ISO strings keep the same UTC instant; naive values fail without a zone; naive datetime/text with explicit IANA localize correctly; bare numeric and date-only timestamps fail; invalid IANA names fail at configuration; Europe/Berlin ambiguous (`2026-10-25 02:30`) and nonexistent (`2026-03-29 02:30`) local clocks fail closed.
- CSV adapter additions: default constructor remains MW + no timezone (Chunk 6 regression). Explicit `PowerUnit.KW` converts `Consumption_kW` `12500 → 12.5 MW`. Default adapter still rejects `Consumption_kW`. `PowerUnit.KW` with an explicit MW header fails. Fuzzy `Consumpton_kW` with `PowerUnit.KW` is accepted with a warning and converted. Naive timestamps succeed only with explicit `source_timezone`. Aware `+04:00` is not overwritten by a configured fallback zone. DST-ambiguous/nonexistent rows fail in isolation; later valid rows survive. Unique raw timestamps/values/headers/zone names must not appear in diagnostics or DLQ metadata.
- Excel adapter additions: explicit kW numeric conversion; default kW rejection; MW/kW header-config mismatch; typed naive datetime + explicit IANA succeeds; typed naive datetime without timezone still fails; aware timestamp strings remain authoritative; numeric Excel timestamps still rejected; boolean MW/kW cells still rejected; ambiguous and nonexistent local datetimes fail; partial success and raw-source privacy are preserved. Existing Chunk 7 cases remain the Excel regression suite.
- Shared mapping tests cover KW-profile exact/fuzzy aliases and reject conflicting MW or energy-like headers.
- Architecture test (`tests/architecture/test_structured_normalization_boundary.py`): the normalization package must not import application/API/ML, pandas/openpyxl, HTTP clients, databases, or LLM/graph libraries. Application ports still expose no `PowerUnit`, `ZoneInfo`, timezone string, or normalization config.

Chunk 9 Consumption duplicate/interval validation tests:

- Primitive tests (`tests/unit/infrastructure/adapters/structured/time_series/`): `IntervalGrid` accepts positive 1-hour and 15-minute intervals; rejects zero/negative intervals and naive anchors; normalizes aware non-UTC anchors to UTC; remains frozen and equality-stable. Different timestamps for one consumer and the same timestamp for different consumers are valid. No grid means no cadence restriction. Exact and conflicting duplicates fail every group member. Canonically equivalent offsets collide. Nonadjacent and 3+ member groups are detected. Valid output keeps source order. Hourly on-grid timestamps succeed; 00:30 fails; pre-anchor timestamps use integer-microsecond modulo; `00:00` + `02:00` is not a missing-gap failure; out-of-order aligned timestamps stay unsorted; a duplicate that is also off-grid is classified as a duplicate only.
- CSV adapter additions: default ingest detects duplicates without an interval grid; conflicting MW values fail closed; different consumers at one instant survive; offset-equivalent aware strings collide; nonadjacent duplicates are removed; an explicit 1-hour UTC grid accepts aligned rows; 00:30 is isolated; a 2-hour gap is allowed; source order of `02:00, 00:00, 01:00` is preserved; kW and explicit timezone paths still work with structural validation; unique consumer/timestamp/value sentinels are absent from diagnostics and DLQ metadata.
- Excel adapter additions: duplicate canonical and typed-naive (explicit timezone) rows fail closed; conflicting duplicates fail; different consumers remain valid; offset-equivalent aware strings collide; explicit hourly grid accepts aligned typed rows; off-grid typed and aware timestamps are isolated; a 2-hour gap is allowed; out-of-order aligned rows keep source order; kW conversion and explicit timezone localization still work; naive timestamps without a configured zone still fail before interval classification; privacy sentinels stay out of outward metadata. Existing Chunk 7/8 cases remain the Excel regression suite.
- Architecture test (`tests/architecture/test_time_series_validation_boundary.py`): the time-series package must not import application/API/ML, pandas/openpyxl, HTTP clients, databases, or LLM/graph libraries. Application ports still expose no `IntervalGrid`, interval-grid `timedelta` cadence config, duplicate policy, source positions, or `ConsumptionGap`. `CachePort` may annotate TTL as `timedelta`; that is not cadence configuration.

Chunk 10 Consumption missing-interval / gap reporting tests:

- Primitive tests (`tests/unit/infrastructure/adapters/structured/time_series/test_gaps.py`): no grid means no gaps; consecutive hours have no gap; `00:00`+`02:00` is one missing interval; `00:00`+`05:00` is one compact gap of 4; two separated gaps stay two objects; 15-minute `00:00`+`00:45` has count 2; multiple consumers keep independent gaps; a complete sibling does not suppress another consumer's gap; a single record and an empty candidate tuple produce no gap; out-of-order candidates still detect the chronological gap while valid output stays in source order; `02:00`+`03:00` infers no leading/trailing coverage; pre-anchor `-03:00`/`-01:00` detects `-02:00`; equivalent offsets use canonical UTC only; a 10-day hourly span is one compact 239-count gap; gap objects are frozen and equality-stable; singular/plural diagnostic messages contain only the count.
- Structural interaction: duplicate rejection of `01:00` between valid `00:00` and `02:00` yields duplicate issues plus one canonical gap; an off-grid `00:30` between `00:00` and `02:00` yields an interval-misaligned issue plus a missing `01:00` gap; duplicate-over-interval precedence still holds and a lone survivor has no gap.
- CSV adapter additions: explicit hourly grid reports a gap diagnostic without DLQ; the same source without a grid reports no gap; a 4-interval span emits one diagnostic; multiple gaps emit multiple diagnostics; different consumers stay independent; duplicate-induced and off-grid-induced gaps keep row DLQ plus a separate gap diagnostic with no extra gap DLQ; out-of-order source order is preserved; kW/timezone paths still work; unique consumer/timestamp/value/anchor sentinels are absent from outward metadata; no synthetic `01:00` record appears.
- Excel adapter additions: corresponding typed/aware hourly gap, no-grid, compact multi-slot, per-consumer, duplicate-induced, off-grid-induced, out-of-order, timezone, kW, no-fake-DLQ, privacy, and no-synthetic-row cases. Existing Chunk 7–9 Excel tests remain the regression suite.
- Architecture test (`tests/architecture/test_time_series_gap_boundary.py`): gap types and coverage windows do not appear on application ingestion ports; the time-series package still has no application/API/ML/file-reader imports.

Chunk 11 filesystem DLQ persistence tests:

- Unit tests (`tests/unit/infrastructure/persistence/test_dlq.py`): `tmp_path` only; no database or broker. Enqueue creates one canonical JSON file; the persistence directory is created when absent; stored JSON round-trips to `DLQRecord`; persisted keys are canonical DLQ/diagnostic fields only; diagnostics and optional `correlation_id` survive; identical retry is idempotent and does not add a second file; same `record_id` with different metadata raises `ConflictError` without overwrite; an opaque/filename-unsafe `record_id` is stored under a SHA-256 filename inside the configured root; create/write/corrupt-file failures become sanitized `DependencyUnavailableError` with no root path, OS text, raw JSON, or secret sentinel; async `enqueue()` offloads blocking work through `asyncio.to_thread`.
- Architecture test (`tests/architecture/test_dlq_persistence_boundary.py`): application `DeadLetterQueuePort` still accepts one canonical `DLQRecord` and exposes no `Path`/raw-payload/database/HTTP/`Any` surface; `FilesystemDeadLetterQueue` may depend on application errors and domain contracts but must not import FastAPI, pandas/openpyxl, HTTP clients, databases, brokers, LangChain/LangGraph/OpenAI, ML libraries, or Consumption CSV/Excel adapters.
- These tests do not cover PostgreSQL DLQ storage, replay, listing, deletion, or payload-reference resolution.

Chunk 12 unstructured document extraction boundary tests:

- Unit tests (`tests/unit/application/ports/test_document_extraction.py`): valid frozen `ExtractedDocumentChunk`; identifier/text whitespace normalization; empty `document_id`/`chunk_id`/text rejected; negative ordinal rejected; `page_number` 0/negative rejected and `None` allowed; valid frozen `DocumentExtractionResult`; collection fields must be tuples; wrong chunk/diagnostic/DLQ types rejected; result/chunk `document_id` mismatch rejected; duplicate `chunk_id` rejected; empty chunks, partial success, and complete extraction failure remain representable; a test-only fake structurally satisfies async `DocumentExtractionPort.extract()` with no raw-document argument.
- Architecture test (`tests/architecture/test_document_extraction_boundary.py`): application extraction module imports none of infrastructure/API/ML, pathlib, pandas/openpyxl, PDF/OCR libraries, HTTP clients, databases, Redis, Qdrant, LangChain/LangGraph/OpenAI, or ML libraries. `extract()` accepts only `self`. DTOs have only normalized provenance fields and no embedding/vector/path/URL/raw-payload/metadata bag.
- Real PDF/OCR fixture and integration tests remain deferred until a concrete infrastructure adapter exists.

Chunk 13 async PostgreSQL/TimescaleDB persistence foundation tests:

- Database settings tests (`tests/unit/test_database_settings.py`): valid `DatabaseSettings`; default and explicit ports; invalid ports rejected; empty host/database/username rejected; positive pool size; zero/negative pool size rejected; non-negative max overflow; negative overflow rejected; positive timeout; zero/negative timeout rejected; sentinel password masked in `repr`/`str`; local `.env` does not contaminate explicit construction (`env_file=None` plus cleared `ENERGY_DB_*` vars). No database process.
- Engine/session factory tests (`tests/unit/infrastructure/persistence/postgres/test_engine.py`): `postgresql+psycopg` URL; host/port/database/user semantics; password retained internally and hidden in safe URL rendering; `AsyncEngine` returned without connecting; `pool_pre_ping` enabled; async session factory with `expire_on_commit=False`; no module-global engine. Engines are disposed in tests.
- Alembic offline/static tests (`tests/unit/infrastructure/persistence/postgres/test_alembic.py`): Alembic config loads; bootstrap revision `0001_bootstrap` remains the root and still creates no canonical business tables; TimescaleDB extension and `energy_trading` schema intent; bootstrap downgrade does not drop the TimescaleDB extension; no hardcoded credentials. Head is the later Consumption revision (see Chunk 14).
- Architecture tests (`tests/architecture/test_postgres_persistence_boundary.py`): domain/application/API do not import SQLAlchemy/psycopg/Alembic or postgres factories; `DatabaseSettings` does not import infrastructure/runtime DB libraries; postgres package does not import FastAPI/agents/ML/ingestion adapters/Redis/Qdrant; `create_app()` does not instantiate an engine.

Chunk 14 Consumption PostgreSQL persistence tests:

- Application port tests (`tests/unit/application/ports/test_consumption_repository.py`): a test-only fake structurally satisfies async `ConsumptionRepositoryPort.save_many`; exact signature; tuple of `ConsumptionRecord`; empty tuple is valid; port annotations expose no SQLAlchemy/session/database/raw-source types; fake can represent success and `ConflictError`.
- Core table metadata tests (`tests/unit/infrastructure/persistence/postgres/test_tables.py`): schema `energy_trading`; table `consumption_observations`; exact canonical columns; timezone-aware non-null timestamp; `DOUBLE PRECISION` MW; composite PK `(consumer_id, timestamp)`; non-negative/finite CHECK; no synthetic ID or provenance columns; no ORM class.
- Repository tests (`tests/unit/infrastructure/persistence/postgres/test_consumption_repository.py`): session-factory doubles only; no live PostgreSQL. Empty input opens no session; one/many records use one transaction; in-call exact duplicates coalesce; in-call differing identity conflicts before DB work; compiled insert uses `ON CONFLICT DO NOTHING` without `DO UPDATE`; exact persisted retry succeeds; differing stored MW is `ConflictError` with rollback; mixed new+conflict rolls back; persisted rows rebuild as `ConsumptionRecord`; corrupt storage fails closed; DBAPI and pool-timeout failures become sanitized `DependencyUnavailableError`; sentinel exception text is not exposed; repository does not create an engine and does not per-row commit.
- Alembic static tests (`tests/unit/infrastructure/persistence/postgres/test_alembic.py`): head is `0002_consumption`; down-revision is `0001_bootstrap`; Consumption hypertable on `timestamp` with compatible `create_hypertable` and no chunk interval; downgrade drops only the Consumption table without `CASCADE`/`DROP EXTENSION`/`DROP SCHEMA`; no credentials.
- Architecture tests (`tests/architecture/test_consumption_repository_boundary.py`): application port has no SQLAlchemy/session/raw-source surface; repository may use SQLAlchemy plus application errors and canonical Consumption; API remains unwired; filesystem DLQ is not PostgreSQL-backed; no generic `Repository`/`UnitOfWork`; no other canonical observation tables.

Chunk 15 PostgreSQL/TimescaleDB service profile and live persistence tests:

- Compose static tests (`tests/architecture/test_postgres_compose_profile.py`): `compose.yaml` exists; image is exactly `timescale/timescaledb:2.29.2-pg17`; no `latest`; service `timescaledb` is gated on profile `postgres`; port is loopback-bound; named volume `timescale-data` with no bind-mounted data directory; healthcheck uses `pg_isready` without a password; password interpolation required; no `POSTGRES_HOST_AUTH_METHOD=trust`; no FastAPI/admin services. Redis, Qdrant, and n8n are separate independently gated Compose services, not asserted absent.
- Marker: `postgres_integration`. Opt-in: `ENERGY_RUN_POSTGRES_INTEGRATION=1`. Live modules skip cleanly when the flag is absent. Default `uv run pytest` does not require Docker.
- Live suite path: `tests/integration/persistence/postgres/`. No testcontainers, Docker SDK, or new Python dependencies. On Windows, Alembic and live tests use `WindowsSelectorEventLoopPolicy` because psycopg async cannot use ProactorEventLoop.
- Live migration tests: PostgreSQL server major version 17; TimescaleDB extension `2.29.2`; Alembic current `0002_consumption`; TimescaleDB extension and `energy_trading` schema exist; `consumption_observations` is a hypertable partitioned by `timestamp`; composite PK `(consumer_id, timestamp)`; non-negative/finite MW CHECK; controlled downgrade to `0001_bootstrap` then restore to head without dropping schema or extension.
- Live repository tests: real `save_many`; UTC instant round-trip from a non-UTC aware input; exact retry remains one row; differing same-identity value is `ConflictError` and preserves the original; mixed new+conflict batch rolls back the new row; concurrent exact retries both succeed with one physical row; concurrent conflicting writers yield one success, one `ConflictError`, one physical row; direct SQL negative MW is rejected by the database constraint.
- **Default pytest remains independent of services.** The integration suite requires an explicitly started local `postgres` profile. No live third-party APIs.

Chunk 16 application cache port boundary tests:

- Unit tests (`tests/unit/application/ports/test_cache.py`): a test-only in-memory fake structurally satisfies async generic `CachePort[ConsumptionRecord]`; it does not inherit an infrastructure base class and is not a production cache. Coverage includes cache miss (`None`), typed canonical round-trip, overwrite of an existing key, replacement establishing a new TTL, deterministic expiry without wall-clock sleep, delete of an existing entry, idempotent delete of a missing key, blank/whitespace-only keys as `InvalidRequestError`, and zero/negative TTL as `InvalidRequestError`.
- Architecture test (`tests/architecture/test_cache_boundary.py`): application `CachePort` imports none of infrastructure/API/ML, FastAPI/Starlette, Redis/`redis.asyncio`, SQLAlchemy/psycopg/Alembic, Qdrant, pandas/openpyxl, HTTP clients, LangChain/LangGraph/OpenAI, or ML libraries. Public annotations expose no Redis client/pool types, `bytes`, `dict`, `Mapping`, or `Any`. API/`create_app()` does not import or construct `CachePort` or a concrete cache.

Chunk 17 async Redis cache infrastructure (offline) tests:

- Redis settings tests (`tests/unit/test_redis_settings.py`): valid `RedisSettings`; default port 6379, DB 0, SSL false, timeout 5s, max connections 10; blank host rejected; host whitespace normalized; invalid ports rejected; negative DB rejected; zero/negative timeout and max connections rejected; optional password; blank password becomes `None`; nonblank password is not stripped; sentinel password absent from `repr`/`str`; `env_file=None` isolates local `.env`. No Redis process.
- Client factory tests (`tests/unit/infrastructure/cache/test_redis_client.py`): `create_redis_client` returns `redis.asyncio.Redis` without connecting; host/port/db/ssl/timeouts/max connections/`decode_responses=False` propagate; password is supplied internally; no module-global client; `aclose()` in cleanup. No PING/GET/SET/DELETE against a server.
- Codec tests (`tests/unit/infrastructure/cache/test_codec.py`): a test-only typed codec structurally satisfies `CacheCodec[ConsumptionRecord]`; encode/decode round-trip; decode failure is `CacheCodecError`.
- `RedisCache` tests (`tests/unit/infrastructure/cache/test_redis_cache.py`): fake Redis command surface only. Structural `CachePort[ConsumptionRecord]`; GET miss is `None`; bytes decode to typed records; SET encodes and uses hashed `energy-trading:cache:<sha256>` keys; raw application-key sentinel is not sent to Redis; whitespace-normalized keys share a backend key; SET uses `px`; 1 µs→1 ms, 1 ms→1 ms, 1500 µs→2 ms, 1 s→1000 ms; overwrite resets TTL; DELETE is one call and `0` is success; blank key and zero/negative TTL are `InvalidRequestError` before I/O; Redis/codec/non-byte GET/unsuccessful SET map to sanitized `DependencyUnavailableError` without sentinel text.
- Architecture tests (`tests/architecture/test_redis_cache_boundary.py`): domain/application/API/ML do not import redis-py or infrastructure cache; `RedisSettings` has no redis-py/runtime engine types; cache package must not import FastAPI, SQLAlchemy, Qdrant, adapters, agents, LangGraph, or ML; codec has no pickle/marshal/shelve/eval/Redis client types; `create_app()` still creates no Redis client.
- Default pytest requires no Redis process.

Chunk 18 Redis service profile and live cache tests:

- Compose static tests (`tests/architecture/test_redis_compose_profile.py`): image is exactly `redis:8.2.9-alpine`; service `redis` is gated on profile `redis`; loopback bind; published host port uses `REDIS_PORT`; `REDIS_PASSWORD` interpolation required; healthcheck uses `redis-cli`, `PONG`, and `REDISCLI_AUTH`; `save ""` and `appendonly no`; no Redis volume/bind mount; no `depends_on`; approved services are `timescaledb`, `redis`, `qdrant`, and `n8n`; no FastAPI/admin/Sentinel/Cluster.
- Marker: `redis_integration`. Opt-in: `ENERGY_RUN_REDIS_INTEGRATION=1`. Live modules skip cleanly when the flag is absent. If the flag is set and Redis is unreachable or unauthenticated, the suite fails rather than skips. Default `uv run pytest` does not require Docker.
- Live suite path: `tests/integration/cache/redis/`. No testcontainers, Docker SDK, fakeredis, or new Python dependencies.
- Localhost-only safety: `REDIS_HOST` must be `127.0.0.1` or `localhost`. Remote hosts fail setup. Tests never call `FLUSHALL`, `FLUSHDB`, or `KEYS *`. Each test uses unique application keys and deletes only those keys.
- Live server tests: authenticated `PING` returns `PONG`; Redis version is exactly `8.2.9`; unauthenticated `PING` raises redis-py `AuthenticationError` without echoing the password; `appendonly` is `no`; `save` is empty.
- Live `RedisCache` tests: set/get round-trip; miss is `None`; overwrite; overwrite resets TTL; delete; missing delete succeeds; real expiry polled within a bounded timeout; key isolation; surrounding whitespace normalization. Codec is test-only UTF-8 `str`; no production default serializer.
- **Default pytest remains independent of services.** The integration suite requires an explicitly started local `redis` profile. Compose Redis is stopped and removed after live validation. No live third-party APIs.

Chunk 19 application document embedding port boundary tests:

- Unit tests (`tests/unit/application/ports/test_document_embedding.py`): valid frozen `DocumentChunkEmbedding`; identifier whitespace normalization; empty/whitespace-only `document_id`/`chunk_id` rejected; `vector` must be a tuple; empty vector rejected; finite positive/negative/zero floats accepted; `NaN` and positive/negative infinity rejected. A test-only fake structurally satisfies async `DocumentEmbeddingPort.embed()` without inheriting an infrastructure base class and is not a production embedder. Coverage includes one-to-one batch mapping, preserved document/chunk identity, preserved input order, empty input returning an empty tuple, equal vector dimensionality in one successful batch, and a sanitized `DependencyUnavailableError`. No NumPy. No service dependency.
- Architecture test (`tests/architecture/test_document_embedding_boundary.py`): application embedding module imports none of infrastructure/API/ML, FastAPI/Starlette, Qdrant, Redis, SQLAlchemy/psycopg/Alembic, NumPy/SciPy/sklearn/torch/tensorflow, transformers/sentence-transformers, OpenAI/LangChain/LangGraph, pandas/openpyxl, HTTP clients, or PDF/OCR libraries. Public annotations expose no `Path`/`bytes`/`dict`/`Mapping`/`Any`/ndarray/provider/Qdrant/metadata/collection/distance/search types. `embed()` accepts only `self` and `chunks: tuple[ExtractedDocumentChunk, ...]` and returns `tuple[DocumentChunkEmbedding, ...]`. API/`create_app()` remains unwired.
- Qdrant live tests still do not exist. No embedding-provider, model-download, or vector-index integration tests are added.

Chunk 20 application document vector indexing boundary tests:

- Unit tests (`tests/unit/application/ports/test_document_vector_index.py`): valid frozen `DocumentVectorIndexEntry` pairing matching `ExtractedDocumentChunk` and `DocumentChunkEmbedding`; nested DTO identity is preserved; mismatched `document_id` or `chunk_id` rejected; same `chunk_id` under one document accepted; dict/tuple replacements rejected. A test-only fake structurally satisfies async `DocumentVectorIndexPort.index()` without inheriting an infrastructure base class and is not a production index. Coverage includes empty no-op without backend I/O, one and many entries, exact retry idempotency, distinct documents sharing a `chunk_id`, in-call exact duplicate coalescing, in-call conflicting identity as `ConflictError`, stored identity with changed text or vector as `ConflictError`, mixed batch vector dimensions as `InvalidRequestError`, and a sanitized `DependencyUnavailableError` without chunk text or vector values. No live service.
- Architecture test (`tests/architecture/test_document_vector_index_boundary.py`): application indexing module imports none of infrastructure/API/ML, FastAPI/Starlette, Qdrant, Redis, SQLAlchemy/psycopg/Alembic, NumPy/SciPy/sklearn/torch/tensorflow, transformers/sentence-transformers, OpenAI/LangChain/LangGraph, pandas/openpyxl, HTTP clients, or PDF/OCR libraries. Public annotations expose no `Path`/`bytes`/`dict`/`Mapping`/`Any`/ndarray/Qdrant/collection/point/distance/search/filter types. `index()` accepts only `self` and `entries: tuple[DocumentVectorIndexEntry, ...]`. The entry DTO has only `chunk` and `embedding` annotated as the existing application DTOs. The port is a non-generic `Protocol` with no retrieval/search operation. API/`create_app()` remains unwired.
- Qdrant live tests still do not exist.

Chunk 21 application document vector retrieval boundary tests:

- Unit tests (`tests/unit/application/ports/test_document_vector_search.py`): valid frozen `DocumentVectorSearchQuery` with a finite vector and positive integer `limit`; `vector` must be a tuple; empty vector rejected; finite positive/negative/zero floats accepted; `NaN` and positive/negative infinity rejected; non-float vector members rejected; zero/negative/`bool` limits rejected. A test-only fake structurally satisfies async `DocumentVectorSearchPort.search()` without inheriting an infrastructure base class and is not a production searcher. Coverage includes zero matches as an empty tuple, one result, multiple results in ranked order, fewer than requested `limit` as valid, never more than `limit`, the same `chunk_id` under different documents as allowed, duplicate `(document_id, chunk_id)` as sanitized `DependencyUnavailableError`, too many backend hits as sanitized `DependencyUnavailableError`, backend failure as sanitized `DependencyUnavailableError`, and incompatible query dimension as sanitized `InvalidRequestError`. Error messages contain neither query-vector values nor retrieved chunk text. Real `ExtractedDocumentChunk` objects are used. No NumPy. No live service.
- Architecture test (`tests/architecture/test_document_vector_search_boundary.py`): application search module imports none of infrastructure/API/ML, FastAPI/Starlette, Qdrant, Redis, SQLAlchemy/psycopg/Alembic, NumPy/SciPy/sklearn/torch/tensorflow, transformers/sentence-transformers, OpenAI/LangChain/LangGraph, pandas/openpyxl, HTTP clients, or PDF/OCR libraries. Public annotations expose no `Path`/`bytes`/`dict`/`Mapping`/`Any`/ndarray/Qdrant/collection/point/distance/score/filter/offset/cursor/query-text types. `search()` accepts only `self` and `query: DocumentVectorSearchQuery` and returns `tuple[ExtractedDocumentChunk, ...]`. The query DTO has only `vector` and `limit`. The port is a non-generic `Protocol` with no indexing or embedding operation. API/`create_app()` remains unwired.
- Qdrant live tests still do not exist.

Chunk 22 async Qdrant client foundation tests (offline):

- Unit tests (`tests/unit/test_qdrant_settings.py`): required host; whitespace normalization; blank host rejected; default port `6333`; explicit valid port; invalid/boolean ports rejected; default `https=False`; optional API key absent; blank API key → `None`; nonblank key retained and absent from `repr`/`str`; default timeout 5; non-positive and boolean timeouts rejected; `env_file=None` plus cleared `QDRANT_*` variables prevent local `.env` contamination; `AppSettings` remains independent of Qdrant configuration. No running Qdrant process.
- Unit tests (`tests/unit/infrastructure/vector_store/qdrant/test_client.py`): `create_qdrant_client()` is synchronous and returns `AsyncQdrantClient`; construction against an unreachable host issues no Qdrant command; tests `await client.close()`; host/port/HTTPS/timeout/API key propagate; `prefer_grpc=False`; `cloud_inference=False`; `check_compatibility=False`; no local/in-memory/path mode; no module-global client; repeated factory calls yield distinct objects; the factory does not close the returned client; secret sentinel is absent from project source. No live server.
- Architecture test (`tests/architecture/test_qdrant_client_boundary.py`): domain/application/API/ML import neither `qdrant_client` nor Qdrant infrastructure. `QdrantSettings` imports none of the Qdrant SDK, FastAPI, Redis, SQLAlchemy, or LLM libraries. The client-foundation module `client.py` may import `qdrant_client` and settings but not FastAPI, SQLAlchemy, Redis, agents, LangGraph, ML, NumPy, FastEmbed, PDF/OCR, application errors, or application document embedding/index/search contracts. `create_app()` remains unwired. Production source rejects `cloud_inference=True`, `models.Document`, and FastEmbed.

Chunk 23 Qdrant document vector index/search adapter tests (offline):

- Unit tests (`tests/unit/infrastructure/vector_store/qdrant/test_document_vector.py`): `QdrantDocumentVectorConfig` collection-name/vector-size validation, whitespace normalization, blank/zero/negative/`bool` rejection, and immutability. Deterministic UUID5 point identity from `(document_id, chunk_id)` (stable, distinct across document or chunk changes, valid UUID, raw identifiers are not point IDs). SHA-256 entry fingerprint is deterministic, hexadecimal, covers text/ordinal/page/vector, and is stored without vector values in payload. Closed payload codec round-trips `ExtractedDocumentChunk` including `page_number=None`, and fails closed on missing/malformed/extra fields and point-ID mismatch. Index adapter structurally satisfies `DocumentVectorIndexPort`: empty input makes no client call; dimension mismatches are `InvalidRequestError` before I/O; in-call exact duplicates coalesce; in-call conflicts are `ConflictError` before I/O; exact retry skips upsert; changed text or vector fingerprint conflicts without write; malformed stored payload is `DependencyUnavailableError`; new writes use deterministic IDs, closed payload, `UpdateMode.INSERT_ONLY`, and `wait=True` (no ordinary overwrite). Post-write verification succeeds; missing post-write points are `DependencyUnavailableError`. Concurrent-writer simulation: INSERT_ONLY plus post-read yields `ConflictError` for different content and success for exact matching content. Backend retrieve/upsert failures (`UnexpectedResponse`, `ResponseHandlingException`, and locked-package `ResourceExhaustedResponse`) become sanitized `DependencyUnavailableError` without exception/text/vector sentinels. Search adapter structurally satisfies `DocumentVectorSearchPort`: wrong query dimension is `InvalidRequestError` before I/O; empty result is `()`; ranking order is preserved; `limit` and numeric query vector propagate; `with_vectors=False`; payload field selection is closed; no filter/threshold/offset/search tuning; scores are ignored; same `chunk_id` under different documents is valid; duplicate identity, malformed payload, point-ID mismatch, extra hits, and backend failure are sanitized `DependencyUnavailableError` without query-vector or retrieved-text leakage. No live Qdrant, no `:memory:` local mode, no sleep/retry loops.
- Architecture test (`tests/architecture/test_qdrant_document_vector_boundary.py`): domain/application/API/ML remain Qdrant-free. `client.py` stays free of application document ports and application errors. The adapter module may import application errors plus extraction/index/search ports and `qdrant_client`, but not FastAPI/Starlette, SQLAlchemy/psycopg/Alembic, Redis, ML, API, agents, LangGraph, OpenAI, pandas/openpyxl, NumPy, PDF/OCR, or the embedding-port module. Production Qdrant code still has no `models.Document` / FastEmbed / `cloud_inference=True`. The adapter module does not reference collection-management operations, `VectorParams`, or `Distance.COSINE`/`Distance.DOT`. `create_app()` constructs neither adapter nor `AsyncQdrantClient`.

Chunk 24 Qdrant service profile and live vector integration tests:

- Compose static tests (`tests/architecture/test_qdrant_live_integration_boundary.py`): image is exactly `qdrant/qdrant:v1.19.1`; service `qdrant` is gated on profile `qdrant`; loopback bind; published host port uses `QDRANT_PORT` and container REST 6333 only; 6334/6335 are not published; `QDRANT_API_KEY` interpolation required as `QDRANT__SERVICE__API_KEY`; no empty read-only key; telemetry disabled; named volume `qdrant-data` mounted at `/qdrant/storage`; no bind mount; no `depends_on` with TimescaleDB, Redis, or n8n; approved services are `timescaledb`, `redis`, `qdrant`, and `n8n`; production Qdrant modules still contain no `create_collection` / `VectorParams` / distance enums; `create_app()` remains unwired.
- Marker: `qdrant_integration`. Opt-in: `ENERGY_RUN_QDRANT_INTEGRATION=1`. Live modules skip cleanly when the flag is absent. If the flag is set and Qdrant is unreachable or unauthenticated, the suite fails rather than skips. Default `uv run pytest` does not require Docker and does not contact Qdrant.
- Live suite path: `tests/integration/infrastructure/vector_store/qdrant/`. No testcontainers, Docker SDK, local `:memory:` Qdrant, or new Python dependencies.
- Localhost-only safety: `QDRANT_HOST` must be `127.0.0.1` or `localhost`. Remote hosts fail setup. Tests never print `QDRANT_API_KEY`.
- Readiness: the fixture constructs `create_qdrant_client(load_qdrant_settings())` and polls authenticated `get_collections()` with a 30-second bound and short async sleeps. This retry loop exists only in test infrastructure.
- Temporary collections: each test receives a unique UUID-named collection created by the fixture with vector size `3` and `Distance.DOT`. That metric is test-only and is not a production embedding-distance selection. The fixture deletes only that collection and always closes the client.
- Live server tests: authenticated `get_collections()` succeeds; an unauthenticated client is rejected with locked-client HTTP `UnexpectedResponse` status 401 or 403 without echoing the API key.
- Live adapter tests: real `QdrantDocumentVectorIndex` / `QdrantDocumentVectorSearch` against the ephemeral collection; new indexing; multiple identities in one call; exact retry remains one point; same-identity changed content is `ConflictError` and preserves the original; ranked search returns `ExtractedDocumentChunk` values with most-relevant first; `limit` is respected; Qdrant scores do not appear on results; empty-collection search returns `()`. No query-text embedding.
- **Default pytest remains independent of services.** The integration suite requires an explicitly started local `qdrant` profile:
  `docker compose --profile qdrant up -d qdrant`
  then `ENERGY_RUN_QDRANT_INTEGRATION=1 uv run pytest -m qdrant_integration`
  then `docker compose --profile qdrant stop qdrant`.
  Do not put a real API key in docs or commands. Compose Qdrant is stopped after live validation. The named volume is not destroyed by default. No live third-party APIs.

Chunk 25 n8n local service foundation and live readiness tests:

- Compose static tests (`tests/architecture/test_n8n_compose_profile.py`): image is exactly `n8nio/n8n:2.37.10`; service `n8n` is gated on profile `n8n`; loopback bind; published host port uses `N8N_HOST_PORT` defaulting to 5678; container `N8N_PORT` remains 5678; custom host port is supported; `N8N_ENCRYPTION_KEY` interpolation required with no default; diagnostics/version/templates/personalization disabled; named volume `n8n-data` mounted at `/home/node/.n8n`; no bind mount; no `depends_on`; approved services are `timescaledb`, `redis`, `qdrant`, and `n8n`; production Python has no n8n SDK; `create_app()` remains unwired; no production `n8n/` or `workflows/` artifacts.
- Marker: `n8n_integration`. Opt-in: `ENERGY_RUN_N8N_INTEGRATION=1`. Live modules skip cleanly when the flag is absent. If the flag is set and n8n is unreachable, the suite fails rather than skips. Default `uv run pytest` does not require Docker and does not contact n8n.
- Live suite path: `tests/integration/infrastructure/orchestration/n8n/`. No testcontainers, Docker SDK, n8n Python SDK, or new Python dependencies. HTTPX is the existing test HTTP client.
- Localhost-only safety: tests contact `127.0.0.1` only. They never print `N8N_ENCRYPTION_KEY`.
- Readiness: the fixture polls `GET /healthz/readiness` with a 60-second bound and short async sleeps. This retry loop exists only in test infrastructure.
- Live server tests: `GET /healthz` returns 200; `GET /healthz/readiness` returns 200. No owner setup, login, workflow creation, credentials, or webhook tests.
- **Default pytest remains independent of services.** The integration suite requires an explicitly started local `n8n` profile:
  `docker compose --profile n8n up -d n8n`
  then `ENERGY_RUN_N8N_INTEGRATION=1 uv run pytest -m n8n_integration`
  then `docker compose --profile n8n stop n8n`.
  Do not put a real encryption key in docs or commands. Compose n8n is stopped after live validation. The named volume is not destroyed by default. No live third-party APIs.

Chunk 26 application agent execution contract tests:

- Unit tests (`tests/unit/application/agents/test_agent_contract.py`): `AgentName` is a `StrEnum` with exactly 13 members; values match the canonical `AGENTS.md` display names including ampersands and capitalization; values are unique and unaliased. A test-only structural fake satisfies generic `AgentPort` without inheriting a production base class. Coverage includes identity, async `run(request)` returning a typed result, `inspect.signature` of `run(self, request)`, coroutine `run`, and a public surface of only `name` plus `run`. No network, Docker, LLM, ML, database, Redis, Qdrant, or n8n.
- Architecture test (`tests/architecture/test_agent_boundary.py`): the shared `AgentPort` module remains free of infrastructure/ML/API/domain, FastAPI/Starlette, LangGraph/LangChain, LLM SDKs, Qdrant/Redis/SQLAlchemy, ML libraries, HTTP clients, or n8n. The agents package as a whole still forbids those outer/vendor prefixes except that the concrete Weather, Hydro, Generation Availability, News Intelligence, and Market Monitoring agents may import canonical `WeatherRecord` / `HydroRecord` / `GenerationAvailabilityRecord` / `NewsEvent` / `MarketPriceRecord`. `AgentPort` is a generic Protocol over `TRequest`/`TResult`, not an ABC. `name` is a property; `run` is async and accepts only `self` and `request`. Public annotations and identifiers exclude `Any`, `dict`, `Mapping`, snapshot/callback/tool/message/model/prompt types. `create_app()` remains unwired.

Chunk 27 application orchestration state contract tests:

- Unit tests (`tests/unit/application/orchestration/test_state.py`): `WorkflowPhase` is a `StrEnum` with exactly 5 members (`contract`, `ingestion`, `forecasting`, `risk_and_bid`, `settlement`); `WorkflowStatus` is a `StrEnum` with exactly 4 members (`pending`, `running`, `succeeded`, `failed`). Valid frozen `WorkflowState` construction; identifier whitespace normalization; blank and non-string identifiers fail; `delivery_date` accepts `date` and rejects `datetime`; wrong phase/status types fail; empty diagnostics succeed; one or many real `AdapterDiagnostic` values succeed; list and wrong-type diagnostics fail; instance is frozen; diagnostics tuple cannot be mutated through the contract; identical values compare equal. No transition/execution methods exist. No live service.
- Architecture test (`tests/architecture/test_orchestration_state_boundary.py`): the orchestration state module imports only stdlib plus canonical `AdapterDiagnostic`. `WorkflowState` is not generic and has no `TypeVar`/`Any`/`dict`/`Mapping` surface. Exact fields are `workflow_id`, `portfolio_id`, `delivery_date`, `correlation_id`, `phase`, `status`, and `diagnostics`. No payload/data/context/metadata/artifacts bag, no agent request/result fields, no retry/fallback/routing fields, no persistence/cache fields, and no LangGraph types. Construction validation only (`__post_init__`). `create_app()` remains unwired. The orchestration package still introduces no agent classes. Package-wide vendor forbids still apply except that `langgraph` is allowed only in the graph module and `AgentName` may be imported by the failure-policy module.

Chunk 28 minimal LangGraph skeleton tests:

- Unit tests (`tests/unit/application/orchestration/test_graph.py`): installed LangGraph satisfies `>=1.2.11,<1.3`; `build_workflow_graph(*, parallel_ingestion_step=..., parallel_ingestion_failure_runtime_handler=...)` returns a compiled graph and requires both keyword-only dependencies; repeated factory calls create independent instances; `WorkflowState` is the state schema. Chunk 43 extends the same file: application nodes are `workflow_entry`, `parallel_ingestion`, and `parallel_ingestion_success_transition`. Chunk 62 adds one Phase-2-specific conditional seam: `ingestion`/`running` → `parallel_ingestion_success_transition` → `END`, and `ingestion`/`failed` → `END`. No phase-specific business nodes; async `ainvoke` does not mutate the original frozen `WorkflowState`. No live service.
- Architecture test (`tests/architecture/test_langgraph_boundary.py`): production `langgraph` imports exist only in `application/orchestration/graph.py`. `state.py`, `failure_policy.py`, agent base, domain, infrastructure, ML, API, and unrelated application ports/use cases remain LangGraph-free. The graph module may import `WorkflowState`, `WorkflowPhase`, `WorkflowStatus`, `InvalidRequestError`, `ParallelIngestionWorkflowStep`, `ParallelIngestionFailureRuntimeHandlingService`, and `advance_after_parallel_ingestion`. It imports no infrastructure/ML/API/agents/LangChain/executor/context/plan/success/`FailurePolicyPort` and does not use messages, reducers, checkpointers, stores, or `AgentPort`/`AgentName`. One Phase-2-specific conditional edge is required after `parallel_ingestion`. AST proves the catch is `BaseExceptionGroup` only. `create_app()` remains unwired.

Chunk 29 application orchestration failure-policy hook tests:

- Unit tests (`tests/unit/application/orchestration/test_failure_policy.py`): `FailureAction` is a `StrEnum` with exactly `RETRY`/`retry`, `FALLBACK`/`fallback`, and `FAIL`/`fail`. Valid frozen `FailurePolicyContext` with no agent; each canonical `AgentName`; whitespace-stripped `error_code`; blank and non-string error codes fail; attempt 1 and later positive attempts succeed; attempt 0/negative/`bool`/non-int fail; `agent_name=None` and actual `AgentName` succeed; arbitrary agent-name strings fail; phase must be `WorkflowPhase`. A test-only structural fake satisfies async `FailurePolicyPort.decide(context)` without inheriting a production base class and can return each `FailureAction`. No sleeping, retries, or fallbacks are executed. No live service.
- Architecture test (`tests/architecture/test_failure_policy_boundary.py`): production `failure_policy.py` imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, or Tenacity. Allowed project imports are `WorkflowPhase` and `AgentName`. Context fields are exactly `phase`, `error_code`, `attempt_number`, and `agent_name`. No `Any`/`dict`/`Mapping`/`TypeVar`, no exception-typed fields, no delay/backoff/max-attempt fields, no `WorkflowState` field, and no callback/callable field. `FailurePolicyPort` is a non-generic Protocol whose only public operation is async `decide`. The contract module remains implementation-free. Chunk 60's `InitialParallelIngestionFailurePolicy` lives in a separate module and does not inherit the Protocol. The skeleton graph does not import or inject the port.

Chunk 30 Weather & Renewable Forecast Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_weather_records.py`): a test-only structural fake satisfies async keyword-only `WeatherRecordSourcePort.fetch`; it does not inherit a production or infrastructure base class. Coverage includes empty tuple, one canonical `WeatherRecord`, multiple records preserving order, and sanitized `DependencyUnavailableError`. No HTTP, Docker, or live provider.
- Unit tests (`tests/unit/application/agents/test_weather_and_renewable_forecast.py`): request location whitespace normalization; aware non-UTC horizons normalize to UTC; naive start/end rejected; equal and reversed horizons rejected; frozen/slots request and result; empty result tuple valid; one and many `WeatherRecord` values preserve order; non-tuple and non-`WeatherRecord` collections rejected. Concrete agent `name` is `AgentName.WEATHER_AND_RENEWABLE_FORECAST`; `run` calls the source exactly once with normalized location/UTC horizon; wraps the returned tuple without reordering; empty source result is valid; `DependencyUnavailableError` propagates with exactly one source invocation. Structural assignment to `AgentPort[WeatherAndRenewableForecastRequest, WeatherAndRenewableForecastResult]` is proven without inheritance.
- Architecture test (`tests/architecture/test_weather_agent_boundary.py`): agent and source-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `fetch`. No concrete provider implementation. Agent public operations remain `name` and `run`. No registry/factory/`Any`/dict payload. `create_app()` and `graph.py` remain unwired. No live provider tests and no Docker requirement.

Chunk 31 Hydro Resources Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_hydro_records.py`): a test-only structural fake satisfies async keyword-only `HydroRecordSourcePort.fetch`; it does not inherit a production or infrastructure base class. Coverage includes empty tuple, one canonical `HydroRecord`, multiple records preserving order, optional canonical hydro fields passing through unchanged, and sanitized `DependencyUnavailableError`. No HTTP, Docker, or live provider. No hydrological formula tests because no calculation exists.
- Unit tests (`tests/unit/application/agents/test_hydro_resources.py`): request resource-ID whitespace normalization; blank/whitespace-only resource ID rejected; aware non-UTC horizons normalize to UTC; naive start/end rejected; equal and reversed horizons rejected; frozen/slots request and result; empty result tuple valid; one and many `HydroRecord` values preserve order; optional canonical hydro fields remain unchanged; non-tuple and non-`HydroRecord` collections rejected. Concrete agent `name` is `AgentName.HYDRO_RESOURCES`; `run` calls the source exactly once with normalized resource/UTC horizon; wraps the returned tuple without reordering; empty source result is valid; `DependencyUnavailableError` propagates with exactly one source invocation. Structural assignment to `AgentPort[HydroResourcesRequest, HydroResourcesResult]` is proven without inheritance.
- Architecture test (`tests/architecture/test_hydro_agent_boundary.py`): agent and source-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `fetch`. No concrete provider implementation. Agent public operations remain `name` and `run`. No registry/factory/`Any`/dict payload. No Generation Availability coupling. `create_app()` and `graph.py` remain unwired for Weather and Hydro. No live provider tests and no Docker requirement.

Chunk 32 Generation Availability Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_generation_availability_records.py`): a test-only structural fake satisfies async keyword-only `GenerationAvailabilityRecordSourcePort.fetch`; it does not inherit a production or infrastructure base class. Coverage includes canonical asset ID and horizons accepted, empty tuple, one canonical `GenerationAvailabilityRecord`, multiple records preserving order, status values passing through unchanged, `available_capacity_mw` and optional `total_capacity_mw` passing through unchanged, and sanitized `DependencyUnavailableError`. No HTTP, Docker, or live provider. No capacity-calculation tests because no calculation exists.
- Unit tests (`tests/unit/application/agents/test_generation_availability.py`): request asset-ID whitespace normalization; blank/whitespace-only asset ID rejected; aware non-UTC horizons normalize to UTC; naive start/end rejected; equal and reversed horizons rejected; frozen/slots request and result; empty result tuple valid; one and many `GenerationAvailabilityRecord` values preserve order; canonical status and capacity fields remain unchanged; non-tuple and non-`GenerationAvailabilityRecord` collections rejected. Concrete agent `name` is `AgentName.GENERATION_AVAILABILITY`; `run` calls the source exactly once with normalized asset/UTC horizon; wraps the returned tuple without reordering; empty source result is valid; `DependencyUnavailableError` propagates with exactly one source invocation and no retry. Structural assignment to `AgentPort[GenerationAvailabilityRequest, GenerationAvailabilityResult]` is proven without inheritance. The agent module exposes no Hydro types.
- Architecture test (`tests/architecture/test_generation_availability_agent_boundary.py`): agent and source-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `fetch`. No concrete provider implementation. Agent public operations remain `name` and `run`. No registry/factory/`Any`/dict payload. Explicit no-Hydro-coupling assertions: the Generation agent must not import `HydroResourcesAgent`, `HydroRecordSourcePort`, or `HydroRecord`. `create_app()` and `graph.py` remain unwired for Weather, Hydro, and Generation. No live provider tests and no Docker requirement.

Chunk 33 News Intelligence Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_news_events.py`): a test-only structural fake satisfies async keyword-only `NewsEventSourcePort.fetch`; it does not inherit a production or infrastructure base class. Coverage includes canonical UTC horizons accepted, empty tuple, one canonical `NewsEvent`, multiple records preserving order, canonical headline/summary/category/severity passing through unchanged, and sanitized `DependencyUnavailableError`. No HTTP, Docker, live provider, LLM, embedding, or scraping. No sentiment/summarization/scoring tests because no inference exists.
- Unit tests (`tests/unit/application/agents/test_news_intelligence.py`): request horizon validation; aware non-UTC horizons normalize to UTC; naive start/end rejected; equal and reversed horizons rejected; frozen/slots request and result; empty result tuple valid; one and many `NewsEvent` values preserve order; canonical fields remain unchanged; non-tuple and non-`NewsEvent` collections rejected. Concrete agent `name` is `AgentName.NEWS_INTELLIGENCE`; `run` calls the source exactly once with the UTC horizon; wraps the returned tuple without reordering; empty source result is valid; `DependencyUnavailableError` propagates with exactly one source invocation and no retry. Structural assignment to `AgentPort[NewsIntelligenceRequest, NewsIntelligenceResult]` is proven without inheritance. The agent does not summarize, classify, or infer sentiment/relevance/impact.
- Architecture test (`tests/architecture/test_news_intelligence_agent_boundary.py`): agent and source-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, scrapers/feed parsers, n8n, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `fetch` returning `tuple[NewsEvent, ...]`. No concrete provider implementation. Agent public operations remain `name` and `run`. No registry/factory/`Any`/dict payload. No embedding/Qdrant/document-vector coupling. `create_app()` and `graph.py` remain unwired for Weather, Hydro, Generation, and News. No live provider tests and no Docker requirement.

Chunk 34 Market Monitoring Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_market_price_records.py`): a test-only structural fake satisfies async keyword-only `MarketPriceRecordSourcePort.fetch`; it does not inherit a production or infrastructure base class. Coverage includes canonical market ID and horizons accepted, empty tuple, one canonical `MarketPriceRecord`, multiple records preserving order, `EnergyPrice` and explicit currency passing through unchanged, optional `volume_mwh` passing through unchanged, missing optional volume remaining `None`, and sanitized `DependencyUnavailableError`. No HTTP, Docker, live provider, ML, or LLM. No DAM cadence, currency-default, or forecasting tests because none of those behaviors exist.
- Unit tests (`tests/unit/application/agents/test_market_monitoring.py`): request market-ID whitespace normalization; blank/whitespace-only market ID rejected; aware non-UTC horizons normalize to UTC; naive start/end rejected; equal and reversed horizons rejected; frozen/slots request and result; empty result tuple valid; one and many `MarketPriceRecord` values preserve order; `EnergyPrice`, explicit currency, and optional volume remain unchanged; missing volume stays `None`; non-tuple and non-`MarketPriceRecord` collections rejected. Concrete agent `name` is `AgentName.MARKET_MONITORING`; `run` calls the source exactly once with normalized market ID/UTC horizon; wraps the returned tuple without reordering; empty source result is valid; `DependencyUnavailableError` propagates with exactly one source invocation and no retry. Structural assignment to `AgentPort[MarketMonitoringRequest, MarketMonitoringResult]` is proven without inheritance. The agent does not generate `PriceForecastPoint` values, convert currency, or assume interval cadence.
- Architecture test (`tests/architecture/test_market_monitoring_agent_boundary.py`): agent and source-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `fetch` returning `tuple[MarketPriceRecord, ...]`. No concrete provider implementation. Agent public operations remain `name` and `run`. No registry/factory/`Any`/dict payload. No `PriceForecastPoint` coupling and no DAM business-rule constants. `create_app()` and `graph.py` remain unwired for Weather, Hydro, Generation, News, and Market. No live provider tests and no Docker requirement.

Chunk 35 parallel ingestion fan-out plan contract tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion.py`): a valid frozen `ParallelIngestionPlan` is constructed from the five real existing request DTO types; supplied request objects are preserved by identity; the public field set is exactly `weather_and_renewable_forecast`, `hydro_resources`, `generation_availability`, `news_intelligence`, and `market_monitoring`; the DTO is frozen/slotted and has no execution helpers; distinct request scopes and horizons are preserved without a cross-request equality policy; no default entity IDs or source scopes are invented. Real application request objects are used. No live service.
- Architecture test (`tests/architecture/test_parallel_ingestion_plan_boundary.py`): production `parallel_ingestion.py` imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. Public plan annotations are exactly the five existing request DTO types. No `Any`/`dict`/`Mapping` payload, callbacks, exception objects, or graph/runtime types. `WorkflowState` shape is unchanged. The skeleton graph and `FailurePolicyPort` remain unwired. `create_app()` remains unwired.

Chunk 36 parallel ingestion successful fan-in contract tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion.py`): a valid frozen `ParallelIngestionSuccess` is constructed from the five real existing result DTO types; supplied result objects are preserved by identity; the public field set is exactly the same five names as the plan; wrong result types fail construction; empty record tuples remain legitimate; distinct result scopes are preserved without a cross-branch identity policy; no execution helpers; no generic status/failure/degraded fields; construction does not invoke agents, ports, infrastructure, or the graph. Real application result objects are used. No live service.
- Architecture test (`tests/architecture/test_parallel_ingestion_plan_boundary.py`): `ParallelIngestionSuccess` annotations are exactly the five existing result DTO types. No generic outcome/result wrapper, `Optional` branch failure, exception object, or runtime type. Plan request annotations remain unchanged. Graph, `FailurePolicyPort`, `WorkflowState`, and `create_app()` remain unwired to both contracts.

Chunk 37 parallel ingestion execution-boundary tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion.py`): a test-only structural fake satisfies `ParallelIngestionExecutionPort` without inheriting a production base class. `execute` is async, accepts a real `ParallelIngestionPlan`, and returns a real `ParallelIngestionSuccess`. Empty record tuples inside valid branch results can flow through. The port exposes only `execute`. The fake can return the exact supplied success aggregate without transformation. No live service. Tests do not encode concurrency or ordering semantics.
- Architecture test (`tests/architecture/test_parallel_ingestion_plan_boundary.py`): `ParallelIngestionExecutionPort` is a non-generic Protocol whose only public operation is async `execute(self, plan: ParallelIngestionPlan) -> ParallelIngestionSuccess`. The contract module still has no concrete executor, `asyncio.gather`/`TaskGroup`/`create_task`, or agent `.run()` invocation. Graph, `FailurePolicyPort`, `WorkflowState`, and `create_app()` remain unwired to the contract. Concurrent execution coverage is Chunk 38. Failure-policy execution, degraded continuation, and graph-integration tests remain planned.

Chunk 38 concurrent parallel-ingestion executor tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_executor.py`): concrete `ConcurrentParallelIngestionExecutor` structurally satisfies `ParallelIngestionExecutionPort` without inheriting the Protocol. Tests inject the five real application agents behind test-only source-port fakes. Coverage includes one invocation per agent with the exact plan request objects, all-success `ParallelIngestionSuccess`, result-DTO identity preservation, empty record tuples, no skipped branches, and no output rewriting. A deterministic concurrency test records branch start events, starts `execute` as a task, waits until all five have started, then releases a shared gate; it does not use wall-clock timing as the primary proof. Failure tests use existing `DependencyUnavailableError`: `execute` does not return `ParallelIngestionSuccess`, sibling gated branches are cancelled, each branch is invoked once (no retry), and no partial/degraded aggregate is fabricated. No live service.
- Architecture test (`tests/architecture/test_parallel_ingestion_executor_boundary.py`): the executor module may import stdlib `asyncio`, the five application agent modules, and the parallel-ingestion contract module. It imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, ML/LLM libraries, or Tenacity. Exactly one production class `ConcurrentParallelIngestionExecutor`. Constructor annotations are exactly the five agent types. `execute` is async `(self, plan: ParallelIngestionPlan) -> ParallelIngestionSuccess`. Production uses `asyncio.TaskGroup` and five `create_task` calls, not `asyncio.gather`/`wait`/`as_completed`/`to_thread`/thread or process pools. No `FailurePolicyPort`, `WorkflowState`, registry/factory, graph import, or `create_app()` wiring. Graph join, failure-policy execution, and degraded-continuation tests remain planned.

Chunk 39 parallel-ingestion workflow-context boundary tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_context.py`): a test-only structural fake satisfies `ParallelIngestionWorkflowContextPort` without inheriting a production base class. Both public methods are async. `resolve_plan` receives the exact caller `workflow_id` (matching published `WorkflowState.workflow_id` typing) and returns the exact preconfigured `ParallelIngestionPlan` by identity. `record_success` receives the exact workflow identity and the exact `ParallelIngestionSuccess` object by identity, returns `None`, and does not transform plan or success. No live service. There is no production fake.
- Architecture test (`tests/architecture/test_parallel_ingestion_context_boundary.py`): the Protocol is non-generic with exactly `resolve_plan` and `record_success`. Annotations match `WorkflowState.workflow_id`, `ParallelIngestionPlan`, `ParallelIngestionSuccess`, and `None`. The module imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, or ML/LLM libraries. No `Any`/`dict`/`Mapping` payload, callbacks, exception objects, persistence sessions, Redis clients, SQL connections, or graph runtime types. The application module contains the Protocol only. `WorkflowState` remains the seven-field snapshot. Graph, executor, `FailurePolicyPort`, and `create_app()` remain unwired. Durable Redis/Postgres/filesystem context classes remain absent. The in-memory adapter is covered by Chunk 44.

Chunk 40 parallel-ingestion workflow-step tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_workflow.py`): a test-only structural context fake and execution-port fake drive `ParallelIngestionWorkflowStep`. Coverage includes exact `resolve_plan → execute → record_success` ordering, `state.workflow_id` propagation, plan/success object identity preservation, original `WorkflowState` returned by identity with all seven fields unchanged, no concrete-executor type dependency, resolve/execute/record short-circuit failure propagation using existing `DependencyUnavailableError`, and no retry. No live service. No `FailurePolicyPort` interaction.
- Architecture test (`tests/architecture/test_parallel_ingestion_workflow_boundary.py`): exactly one production class `ParallelIngestionWorkflowStep`. Constructor annotations are exactly the two published ports. `run` is async `(self, state: WorkflowState) -> WorkflowState`. The module imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, ML/LLM libraries, Tenacity, the five agents, `ConcurrentParallelIngestionExecutor`, or `FailurePolicyPort`. `run` does not construct, replace, or mutate `WorkflowState`. Executor, context Protocol, `FailurePolicyPort`, and `create_app()` remain unwired to constructing the step. LangGraph may import the step for injection. Production/durable context storage, failure-policy execution, and degraded-continuation tests remain planned.

Chunk 41 LangGraph Phase 2 workflow-step invocation tests:

- Unit tests (`tests/unit/application/orchestration/test_graph.py`): a test-only recording fake is injected as `parallel_ingestion_step`. Chunk 41 coverage of keyword-only factory dependency and Phase 2 delegation remains. Chunk 43 extends the same file with the success-transition node, success path ending `forecasting`/`running`, and failure short-circuit of that node. No live service. The real five-agent executor is not constructed.
- Architecture test (`tests/architecture/test_langgraph_boundary.py`): factory annotations are exactly `parallel_ingestion_step: ParallelIngestionWorkflowStep` and `parallel_ingestion_failure_runtime_handler: ParallelIngestionFailureRuntimeHandlingService`. The module imports `WorkflowState`, the step class, the runtime failure handler, and `advance_after_parallel_ingestion`. It does not import the executor, context port, plan, success, five agents, or `FailurePolicyPort`. AST proves three `add_node` calls, three `add_edge` calls, one `add_conditional_edges` call, node name `parallel_ingestion_success_transition`, one call to the published transition function, and no `dataclasses.replace` reimplementation. `create_app()` remains unwired.

Chunk 42 parallel-ingestion success-transition tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_transition.py`): `advance_after_parallel_ingestion` is synchronous. Coverage includes a valid `ingestion`/`running` snapshot with non-empty diagnostics producing a different `forecasting`/`running` object; input non-mutation; preservation of workflow ID, portfolio ID, delivery date, correlation ID, diagnostics tuple identity, and diagnostic object identities; no extra fields; parameterized rejection of every non-ingestion phase; parameterized rejection of every non-running status while phase is ingestion (`pending`, `succeeded`, `failed`); sanitized `InvalidRequestError` without workflow identity or diagnostic text; and deterministic equivalent outputs from equivalent inputs. No I/O, mocks, live services, or async tests.
- Architecture test (`tests/architecture/test_parallel_ingestion_transition_boundary.py`): exactly one public function `advance_after_parallel_ingestion(state: WorkflowState) -> WorkflowState`. The module uses `dataclasses.replace` and imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, ML/LLM libraries, the five agents, parallel-ingestion plan/success/execution/context/executor/step types, or `FailurePolicyPort`. No Protocol, factory, registry, or generic state machine. `WorkflowState` remains the seven-field snapshot with no transition methods. `parallel_ingestion_workflow.py` remains LangGraph-free and unwired to the transition. `graph.py` may import and call the published function from a thin node and must not reimplement phase/status assignment. `create_app()` remains unwired. No live-service requirements.

Chunk 43 LangGraph Phase 2 success-transition wiring tests:

- Unit tests (`tests/unit/application/orchestration/test_graph.py`): injected recording fake for `ParallelIngestionWorkflowStep`. Coverage includes success topology through `parallel_ingestion_success_transition`, successful `ainvoke` from `ingestion`/`running` ending `forecasting`/`running` with identity/date/diagnostics preserved, step seeing the original `ingestion`/`running` snapshot once, `astream(..., stream_mode="updates")` proving entry then ingestion then transition, step `DependencyUnavailableError` from both `ainvoke` and `astream` with exactly one step call, and `astream` proving `parallel_ingestion_success_transition` does not run after a failed non-group step. Chunk 62 supersedes the previous invalid-transition-precondition graph case with fail-closed routing for unexpected post-Phase-2 states. No live service. The real five-agent executor is not constructed.
- Architecture test (`tests/architecture/test_langgraph_boundary.py`): updated for the third node and published-transition import. See Chunk 41 architecture-test note above for the current allowed/forbidden surface.

Chunk 44 in-memory parallel-ingestion workflow-context adapter tests:

- Unit tests (`tests/unit/infrastructure/orchestration/test_parallel_ingestion_context.py`): real `ParallelIngestionPlan` / `ParallelIngestionSuccess` DTOs. Coverage includes known-plan object identity, unknown-plan sanitized `ResourceNotFoundError` without a workflow-ID sentinel, caller mapping mutation isolation, first `record_success`, same-object and value-equal retries, conflict on differing success with original remaining authoritative, independent workflow identities, concurrent equal retries through `asyncio.TaskGroup` without sleeps, and structural assignment to `ParallelIngestionWorkflowContextPort` without inheriting the Protocol. No live service.
- Architecture test (`tests/architecture/test_in_memory_parallel_ingestion_context_boundary.py`): one production class, no Protocol/ABC inheritance, constructor `plans: Mapping[str, ParallelIngestionPlan]`, public operations only `resolve_plan` / `record_success`, `asyncio.Lock` on writes, no Redis/CachePort/SQLAlchemy/Qdrant/LangGraph/API/agents/executor/workflow-step imports, application port remains infrastructure-free, graph and `create_app()` remain unwired. No live-service requirements.

Chunk 45 parallel-ingestion terminal-failure-transition tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_transition.py`): `fail_parallel_ingestion` is synchronous. Coverage includes a valid `ingestion`/`running` snapshot with non-empty diagnostics producing a different `ingestion`/`failed` object; input non-mutation; preservation of workflow ID, portfolio ID, delivery date, correlation ID, diagnostics tuple identity, and diagnostic object identities; no extra fields; parameterized rejection of every non-ingestion phase; parameterized rejection of every non-running status while phase is ingestion (`pending`, `succeeded`, `failed`); already-failed ingestion snapshots cannot be failed again; sanitized `InvalidRequestError` without workflow identity or diagnostic text; deterministic equivalent outputs from equivalent inputs; and success/failure policy separation (`advance_after_parallel_ingestion` → `forecasting`/`running`, `fail_parallel_ingestion` → `ingestion`/`failed`). No I/O, mocks, live services, or async tests.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_transition_boundary.py`): exactly one public function `fail_parallel_ingestion(state: WorkflowState) -> WorkflowState`. The module uses `dataclasses.replace` and imports none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, ML/LLM libraries, the five agents, parallel-ingestion plan/success/execution/context/executor/step types, `FailurePolicyPort`, or `ParallelIngestionFailureDecisionService`. No Protocol, factory, registry, generic state machine, exception parameter, or diagnostic construction. `WorkflowState` remains the seven-field snapshot with no transition methods. The failure-transition module remains LangGraph-free and separate from the success-transition module. `graph.py` does not import or call `fail_parallel_ingestion`. `FailurePolicyPort` remains unwired. `create_app()` remains unwired. No live-service requirements.

Chunk 46 parallel-ingestion failure-policy decision tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_decision.py`): `ParallelIngestionFailureDecisionService` is async. Coverage includes a structural recording fake that does not inherit `FailurePolicyPort`; parameterized pass-through of every published `FailureAction`; exact supplied `FailurePolicyContext` object identity; exactly-once policy invocation; published `decide` signature alignment with `FailurePolicyPort.decide`; and existing application-exception propagation (`DependencyUnavailableError`) with one policy call and no default `FailureAction`. The service has no `WorkflowState` dependency. No I/O, live services, retry, fallback, sleep, or call to `fail_parallel_ingestion`.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_decision_boundary.py`): one production class, constructor `policy: FailurePolicyPort`, public operation only `async decide(self, context: FailurePolicyContext) -> FailureAction`. The module may import existing `FailureAction` / `FailurePolicyContext` / `FailurePolicyPort`. It imports none of LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, the five agents, plan/success/execution/context/executor/step types, `WorkflowState`, `fail_parallel_ingestion`, the success-transition function, or `build_parallel_ingestion_failure_policy_context`. AST asserts exactly one `.decide` call with the supplied context, no `If`/`Match`/`For`/`While`/`Try`, no concrete policy construction, no sleep/backoff, and no graph/workflow/transition/executor wiring. `create_app()` remains unwired. No live-service requirements.

Chunk 47 parallel-ingestion failure-policy context-construction tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_context.py`): `build_parallel_ingestion_failure_policy_context` is synchronous. Coverage includes exact published-field construction with sentinel `WorkflowPhase`, `error_code`, `attempt_number`, and `AgentName` identity; optional `agent_name` remaining `None` without fabricating an identity; deterministic value-equivalent outputs from equivalent inputs; preservation of published constructor validation (blank `error_code`, non-`WorkflowPhase` phase, non-positive `attempt_number`, string `agent_name`); and `inspect.signature` proof that caller facts are keyword-only, required fields have no speculative defaults, and `agent_name` keeps the published optional `None`. No I/O, live services, raw-exception fixtures, retry, or action execution.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_context_boundary.py`): exactly one public function, no public class. Allowed imports are `FailurePolicyContext`, `WorkflowPhase`, and `AgentName`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, `FailureAction`/`FailurePolicyPort`, `WorkflowState`, `fail_parallel_ingestion`, `ParallelIngestionFailureDecisionService`, five agents, traceback/`Exception`/`BaseException` parameters, `try`, branching, sleep/backoff, and graph/workflow/decision/transition wiring. `create_app()` remains unwired. No live-service requirements.

Chunk 48 parallel-ingestion terminal FAIL action-execution tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_action.py`): `execute_parallel_ingestion_failure_action` is synchronous. Coverage includes valid `ingestion`/`running` plus `FailureAction.FAIL` producing a new `ingestion`/`failed` snapshot; input non-mutation; identity/date/correlation/diagnostic preservation; value-equivalence to `fail_parallel_ingestion(state)` without duplicating transition logic; invalid-state `InvalidRequestError` from the published transition propagating unchanged; `RETRY` and `FALLBACK` as existing `InvalidRequestError` with exact sanitized not-implemented messages and no transition to `FAILED`; and parameterization proving every current `FailureAction` member is handled. No I/O, live services, retry, sleep, fallback target, or diagnostic construction.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_action_boundary.py`): exactly one public function, no public class. Allowed imports are `FailureAction`, `WorkflowState`, `fail_parallel_ingestion`, and `InvalidRequestError`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, `FailurePolicyContext`/`FailurePolicyPort`, `ParallelIngestionFailureDecisionService`, the context builder, the success transition, five agents, plan/success/execution/context/executor/step types, retry loops, sleep/backoff, diagnostic construction, and duplicated phase/status mutation. Exactly one call to `fail_parallel_ingestion` on the terminal branch. `graph.py`, the decision service, the context builder, `fail_parallel_ingestion`, `failure_policy.py`, and `create_app()` remain unwired. No live-service requirements.

Chunk 49 prepared parallel-ingestion failure-handling composition tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_handling.py`): `ParallelIngestionFailureHandlingService.handle` is async and keyword-only. Coverage includes a structural recording fake through the real `ParallelIngestionFailureDecisionService`; `FAIL` producing `ingestion`/`failed` with identity/date/correlation/diagnostic preservation and value-equivalence to `fail_parallel_ingestion(state)`; exactly-once policy invocation with the exact supplied `FailurePolicyContext`; `RETRY` and `FALLBACK` as existing `InvalidRequestError` with exact sanitized not-implemented messages; policy-exception identity preservation without reaching the action executor; and invalid-state `FAIL` preserving published terminal-transition validation. No I/O, live services, retry, sleep, fallback target, raw-exception capture, or diagnostic construction.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_handling_boundary.py`): one production class, constructor `decision_service: ParallelIngestionFailureDecisionService`, public operation only keyword-only `async handle(*, state: WorkflowState, context: FailurePolicyContext) -> WorkflowState`. The module may import `WorkflowState`, `FailurePolicyContext`, `ParallelIngestionFailureDecisionService`, and `execute_parallel_ingestion_failure_action`. It imports none of LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, `FailurePolicyPort`, `fail_parallel_ingestion`, or `build_parallel_ingestion_failure_policy_context`. AST asserts exactly one `.decide(context)` call, exactly one `execute_parallel_ingestion_failure_action(state=state, action=action)` call, no `If`/`Match`/`For`/`While`/`Try`, no sleep/backoff/TaskGroup, and no graph/workflow/API wiring. `create_app()` remains unwired. No live-service requirements.

Chunk 50 parallel-ingestion agent-failure attribution tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_agent_failure.py` and executor extensions): `ParallelIngestionAgentFailure` requires canonical `AgentName`, preserves identity, and emits a sanitized message without a custom raw-exception field. Weather and each of the other four Phase 2 agents map to the correct `AgentName` through the real `ConcurrentParallelIngestionExecutor`. `__cause__` is the exact sentinel. Sibling TaskGroup cancellation is not reclassified as agent failure. Two real failures retain independent attribution. All-success still returns the five typed results. No I/O, live services, retry, sleep, ExceptionGroup flattening, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_agent_failure_boundary.py`): one production class, constructor `agent_name: AgentName` only. Allowed import is canonical `AgentName`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, `WorkflowState`, failure-policy contracts/services, traceback/payload fields, and generic failure hierarchy. Executor may import the exception; `graph.py` and `create_app()` remain unwired. No live-service requirements.

Chunk 51 parallel-ingestion ExceptionGroup attributed-leaf extraction tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_exception_group.py`): `extract_parallel_ingestion_agent_failures` is synchronous. Coverage includes one attributed leaf returned by identity; multiple flat attributed leaves in left-to-right order; nested groups in deterministic depth-first left-to-right order; two distinct same-`AgentName` failures retained without deduplication; `__cause__` identity preserved on the exact original wrapper; unattributed `RuntimeError` leaves fail closed as `InvalidRequestError` with the stable sanitized message and no raw-text leakage; the same fail-closed behavior at nested depth and for an only-unexpected leaf; a nested `BaseExceptionGroup` of attributed leaves is traversed; a direct `CancelledError` leaf is unattributed and fails closed. No I/O, live services, retry, sleep, error-code mapping, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_exception_group_boundary.py`): exactly one public function, no public class, Protocol, ABC, registry, or factory. Input is an exception-group type; return type is `tuple[ParallelIngestionAgentFailure, ...]`. Allowed imports are `InvalidRequestError` and `ParallelIngestionAgentFailure`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, `WorkflowState`, failure-policy contracts/services, five agents, executor, `.split()` / `.subgroup()` / `.derive()`, exception-text inspection, and error-code mapping. Graph, executor, the sanitized-fact classifier, and `create_app()` remain unwired. No live-service requirements.

Chunk 52 parallel-ingestion sanitized failure-fact classification tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_fact.py`): `ParallelIngestionFailureFact` is frozen with exactly `agent_name` and `error_code`. Coverage includes canonical `AgentName` preservation, error-code trimming, blank/non-string error-code rejection, no extra public payload fields, representative `ApplicationError` subclasses (plus a test-only custom subclass) reusing the published application error code, unexpected `RuntimeError` / `ValueError` / vendor-shaped causes collapsing to `parallel_ingestion_unexpected_failure` without leaking sensitive text or class names, missing-cause classification succeeding with that unexpected code, `__cause__` identity preserved on the attribution wrapper, and parameterization over all five Phase 2 `AgentName` values. No I/O, live services, retry, sleep, multi-failure selection, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_fact_boundary.py`): one production DTO class and one public classification function. DTO fields are exactly `agent_name` and `error_code`; the DTO is frozen. Allowed imports are stdlib dataclass, `AgentName`, `ApplicationError`, and `ParallelIngestionAgentFailure`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `WorkflowState`, failure-policy contracts/services, context builder, action executor, ExceptionGroup extractor, executor implementation, five agents, traceback inspection, exception-message parsing, and exception-class-name-derived codes. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 53 parallel-ingestion tuple-level failure-fact classification tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_classification.py`): `classify_parallel_ingestion_agent_failures` is synchronous. Coverage includes empty tuple remaining `()`; one attributed failure becoming exactly one `ParallelIngestionFailureFact` with canonical `AgentName` and the existing application error code; multiple failures classified in encounter order; duplicate agent identities retained without collapsing; mixed error codes classified independently through the existing one-leaf classifier; unexpected/non-application and missing causes collapsing to `parallel_ingestion_unexpected_failure`; original `ParallelIngestionAgentFailure` objects and `__cause__` identity preserved; equivalent input tuples producing value-equivalent output tuples; and monkeypatched proof that each element is delegated to `classify_parallel_ingestion_agent_failure`. No I/O, live services, retry, sleep, primary-failure selection, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_classification_boundary.py`): exactly one public function, no public class, Protocol, ABC, registry, or factory. Input is `tuple[ParallelIngestionAgentFailure, ...]`; return type is `tuple[ParallelIngestionFailureFact, ...]`. Allowed imports are `ParallelIngestionAgentFailure`, `ParallelIngestionFailureFact`, and `classify_parallel_ingestion_agent_failure`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `ApplicationError`, `WorkflowState`, failure-policy contracts/services, context builder, action executor, ExceptionGroup extractor, executor implementation, five agents, ExceptionGroup traversal, `__cause__` inspection, sorting/deduplication/grouping/selection machinery, and a second error-code mapping. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 54 parallel-ingestion failure-fact selection-contract tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_selection.py`): a test-only structural fake satisfies `ParallelIngestionFailureSelectionPort` without inheriting it. Coverage includes assignment to the Protocol type; exact `select(facts: tuple[ParallelIngestionFailureFact, ...]) -> ParallelIngestionFailureFact` annotations; the only public operation is synchronous `select`; the exact input tuple object is received unchanged; the fake returns the exact existing `ParallelIngestionFailureFact` instance without reconstruction; duplicate `AgentName` values and mixed error codes remain representable in the input tuple; and no exception, `WorkflowState`, attempt number, policy context, or `FailureAction` is required. The fake's predetermined result is test-local wiring, not a production first/last/priority rule. No I/O, live services, retry, sleep, concrete selector, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_selection_boundary.py`): exactly one public production Protocol, no public function, concrete class, enum, registry, factory, or selector implementation. Allowed imports are typing `Protocol` and `ParallelIngestionFailureFact`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `ApplicationError`, `WorkflowState`, failure-policy contracts/services, context builder, action executor, attribution/extraction/classification, executor implementation, five agents, `[0]`/`[-1]` indexing, `min`/`max`/`sorted`, priority tables, and winner semantics. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 55 parallel-ingestion attempt-number source-contract tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_attempt_number.py`): a test-only structural fake satisfies `ParallelIngestionAttemptNumberPort` without inheriting it. Coverage includes assignment to the Protocol type; exact async `get_attempt_number(self, workflow_id: str) -> int` annotations; the only public operation is `get_attempt_number`; the exact workflow-ID string is received unchanged; the fake may return `1` and another positive value such as `3` without production transformation; no `WorkflowState`, failure fact, policy context, `FailureAction`, `AgentName`, or exception is required; and no increment/reset/set/record/begin/complete write operation exists. The fake's predetermined result is test-local wiring, not a production tracking rule. No I/O, live services, retry, sleep, concrete tracker, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_attempt_number_boundary.py`): exactly one public production Protocol, no public function, concrete class, enum, registry, factory, or tracker implementation. Allowed import is typing `Protocol`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `ApplicationError`, `WorkflowState`/`WorkflowPhase`/`WorkflowStatus`, failure-policy contracts/services, context builder, action executor, attribution/extraction/classification/selection, executor implementation, five agents, increment/reset/set/record write methods, and storage backends. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 56 parallel-ingestion failure-policy context-resolution tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_context_resolution.py`): a test-only structural selector fake and a test-only structural attempt-number fake satisfy the published ports without inheriting them. Coverage includes assignment through the service constructor; keyword-only `async resolve(*, workflow_id, phase, facts) -> FailurePolicyContext`; exact-once `select` of the exact input facts tuple; exact-once `get_attempt_number` of the exact workflow ID; selection before attempt lookup; selected-fact `agent_name`/`error_code` and supplied `phase` preserved on the resulting context; predetermined positive attempt numbers such as `3`; published-builder delegation; selector failure propagating unchanged without attempt lookup; attempt-source failure propagating unchanged without context construction; existing builder validation for invalid attempt `0` propagating unchanged; input facts tuple and selected fact identity/non-mutation; and no call to `FailurePolicyPort`, the decision service, the action executor, or the handling service. The fakes' predetermined results are test-local wiring, not production selection or tracking rules. No I/O, live services, retry, sleep, concrete selector, concrete tracker, or policy invocation.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_context_resolution_boundary.py`): exactly one public production class, no public Protocol/function/enum/registry/factory. Constructor injects exactly the published selection and attempt-number ports. The only public operation is keyword-only async `resolve`. Allowed imports are `WorkflowPhase`, `FailurePolicyContext`, `ParallelIngestionFailureFact`, the two ports, and `build_parallel_ingestion_failure_policy_context`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `WorkflowState`, `FailureAction`/`FailurePolicyPort`, decision/handling services, action executor, attribution/extraction/classification, executor implementation, workflow step/context, five agents, try/except fallback, `[0]`/`[-1]` indexing, sorting, min/max, deduplication, priority tables, attempt defaulting/mutation, and direct `FailurePolicyContext(...)` construction. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 57 parallel-ingestion strict single-failure selector tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_strict_single_failure_selector.py`): `StrictSingleParallelIngestionFailureSelector` satisfies `ParallelIngestionFailureSelectionPort` structurally without inheriting it. Coverage includes exact `select(facts: tuple[ParallelIngestionFailureFact, ...]) -> ParallelIngestionFailureFact`; exactly one fact returning the same instance (`is`) with preserved `agent_name`/`error_code` and no DTO reconstruction across Phase 2 `AgentName` values; empty tuple raising sanitized `InvalidRequestError`; two and three-or-more facts raising the same sanitized error without first/last winner leakage; duplicate agent identities rejecting; value-equal duplicate facts rejecting without deduplication; mixed agents/error codes rejecting without priority; input tuple and fact objects remaining unchanged; deterministic exact-one success; and deterministic multi-fact fail-closed behavior. Chunk 56 integration coverage injects the concrete selector plus a test-only attempt-number fake into `ParallelIngestionFailureContextResolutionService`: one fact yields a valid `FailurePolicyContext` with selected agent, error code, attempt `2`, and supplied phase; two facts raise `InvalidRequestError` without calling the attempt-number fake. No I/O, live services, retry, sleep, ranking, policy invocation, or graph wiring.
- Architecture test (`tests/architecture/test_parallel_ingestion_strict_single_failure_selector_boundary.py`): exactly one public production class, only public operation `select`, no Protocol/helper/enum/registry/factory. Allowed imports are `InvalidRequestError` and `ParallelIngestionFailureFact`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `WorkflowState`/`WorkflowPhase`/`WorkflowStatus`, `FailureAction`/`FailurePolicyContext`/`FailurePolicyPort`, decision/handling/action/builder/resolution, attempt-number port, attribution/extraction/classification, executor, workflow step, five agents, graph, `sorted`/`min`/`max`/`.sort`, priority mappings, sets for deduplication, ranking dictionaries, and counters. A cardinality guard equivalent to `len(facts) == 1` is required before returning a fact. Graph topology and `WorkflowState` remain unchanged. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 58 parallel-ingestion initial attempt-number source tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_initial_attempt_number_source.py`): `InitialParallelIngestionAttemptNumberSource` satisfies `ParallelIngestionAttemptNumberPort` structurally without inheriting it. Coverage includes exact async `get_attempt_number(self, workflow_id: str) -> int`; exact literal `1` across distinct workflow IDs; deterministic repeated calls for the same and different identities; interleaved workflow A/B calls remaining `1` with no accumulated counter; result independent of ID formatting including empty string; no public mutation methods; and real Chunk 56+57 composition injecting the strict selector plus this source into `ParallelIngestionFailureContextResolutionService` so one fact yields `FailurePolicyContext.attempt_number == 1` while two facts still fail closed at the selector. No I/O, live services, retry, sleep, tracking, persistence, policy invocation, or graph wiring.
- Architecture test (`tests/architecture/test_parallel_ingestion_initial_attempt_number_source_boundary.py`): exactly one public production class, only public operation `get_attempt_number`, no Protocol/helper/enum/registry/factory/tracker. No production imports. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `WorkflowState`/`WorkflowPhase`/`WorkflowStatus`, failure facts, selector, context-resolution service, `FailureAction`/`FailurePolicyContext`/`FailurePolicyPort`, decision/handling services, executor, agents, graph, dictionaries/sets/counters, increment/reset/set/record methods, persistence, locks, timestamps, and randomness. The production method returns the literal integer `1` with no arithmetic. Graph topology and `WorkflowState` remain unchanged. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 59 parallel-ingestion failure-context preparation tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_context_preparation.py`): real extractor, classifier, `StrictSingleParallelIngestionFailureSelector`, `InitialParallelIngestionAttemptNumberSource`, and `ParallelIngestionFailureContextResolutionService` are composed into `ParallelIngestionFailureContextPreparationService`. Coverage includes keyword-only `async prepare(*, workflow_id, phase, failure_group) -> FailurePolicyContext`; one attributed `ApplicationError` yielding the supplied phase, original application error code, attributed agent name, and `attempt_number == 1`; unexpected non-application cause collapsing to `parallel_ingestion_unexpected_failure` without leaking raw message/type/repr; nested ExceptionGroup reuse of existing traversal; two attributed failures failing closed with existing `InvalidRequestError` and no first/last winner; unattributed group leaf propagating the extractor error without reaching context resolution; exact workflow ID/phase preservation and non-mutation of the input group; exact-once delegation of extraction, classification, and resolve; classification failure propagating without resolution; and no call to `FailurePolicyPort`, the decision service, the action executor, the handling service, or `fail_parallel_ingestion`. No I/O, live services, retry, sleep, policy invocation, or graph wiring.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_context_preparation_boundary.py`): exactly one public production class, no public Protocol/function/enum/registry/factory. Constructor injects exactly the published context-resolution service and does not construct a selector, attempt source, or resolution service. The only public operation is keyword-only async `prepare`. Allowed imports are `WorkflowPhase`, `FailurePolicyContext`, the extractor, the tuple classifier, and the context-resolution service. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `WorkflowState`, `FailureAction`/`FailurePolicyPort`, decision/handling services, action executor, selector, attempt-number port, initial attempt source, executor, workflow step, five agents, `.exceptions` traversal, `__cause__` inspection, direct `ParallelIngestionFailureFact`/`FailurePolicyContext` construction, `[0]`/`[-1]` indexing, sorting, min/max, deduplication, and hardcoded attempt `1`. Graph topology and `WorkflowState` remain unchanged. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 60 parallel-ingestion initial terminal-fail failure-policy tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_initial_failure_policy.py`): `InitialParallelIngestionFailurePolicy` satisfies `FailurePolicyPort` structurally without inheriting it. Coverage includes exact async `decide(self, context: FailurePolicyContext) -> FailureAction`; `FailureAction.FAIL` for every valid `WorkflowPhase`, representative error codes, positive attempt numbers, and optional `AgentName` values including `None`; result independent of context fields; deterministic repeated calls; non-mutation of the supplied frozen context; no public configuration/mutation methods; and real preparation → context → policy composition so one attributed `ApplicationError` yields the expected phase, error code, agent, and attempt `1`, then `FAIL`, without calling the decision service, handling service, action executor, or `fail_parallel_ingestion`. No I/O, live services, retry, sleep, field-dependent branching, or graph wiring.
- Architecture test (`tests/architecture/test_parallel_ingestion_initial_failure_policy_boundary.py`): exactly one public production class, only public operation `decide`, no Protocol/helper/enum/registry/factory/rule table. Allowed imports are `FailureAction` and `FailurePolicyContext`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, `FailurePolicyPort` inheritance, `WorkflowState`, decision/handling services, action executor, selector, attempt source, preparation/resolution services, extractor, classifier, executor, workflow step, five agents, graph, `if`/`match` branching, dictionaries/sets, `sorted`/`min`/`max`, and `FailureAction.RETRY`/`FALLBACK`. Production `decide` returns exactly `FailureAction.FAIL` without inspecting context fields. Graph topology and `WorkflowState` remain unchanged. Graph and `create_app()` remain unwired. No live-service requirements.

Chunk 61 parallel-ingestion runtime failure-handling composition tests:

- Unit tests (`tests/unit/application/orchestration/test_parallel_ingestion_failure_runtime_handling.py`): real `StrictSingleParallelIngestionFailureSelector`, `InitialParallelIngestionAttemptNumberSource`, `ParallelIngestionFailureContextResolutionService`, `ParallelIngestionFailureContextPreparationService`, `InitialParallelIngestionFailurePolicy`, and the published decision/handling/action path are composed into `ParallelIngestionFailureRuntimeHandlingService`. Coverage includes keyword-only `async handle(*, state, failure_group) -> WorkflowState`; one attributed Phase 2 failure from a valid `ingestion`/`running` snapshot yielding terminal `ingestion`/`failed` with identity/date/correlation/diagnostic preservation matching `fail_parallel_ingestion`; exact preparation-then-handling order via recording doubles; preparation failure propagating unchanged without handling; two attributed failures failing closed with existing `InvalidRequestError` and no handling; unattributed group leaves propagating the extractor error without handling; handling failure propagating unchanged after preparation; and execution without any LangGraph object. No I/O, live services, retry, sleep, policy duplication, action duplication, or graph wiring.
- Architecture test (`tests/architecture/test_parallel_ingestion_failure_runtime_handling_boundary.py`): exactly one public production class, no public Protocol/function/enum/registry/factory/result wrapper. Constructor injects exactly the published preparation and handling services. The only public operation is keyword-only async `handle`. Allowed imports are `WorkflowState`, `ParallelIngestionFailureContextPreparationService`, and `ParallelIngestionFailureHandlingService`. Forbidden: LangGraph/LangChain, infrastructure, API, ML, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, LLM/ML libraries, Tenacity, selector, attempt-number port/source, initial failure policy, extractor, classifier, context builder, decision service, action executor, `fail_parallel_ingestion`, `FailureAction` branching, `.exceptions` traversal, `__cause__` inspection, direct failed-`WorkflowState` construction, five agents, executor, workflow step, and graph. `handle` awaits `prepare` exactly once then handling `handle` exactly once. The graph may import this outer service and must not import its internals. `create_app()` remains unwired. No live-service requirements.

Chunk 62 LangGraph Phase 2 single-failure terminal routing tests:

- Unit tests (`tests/unit/application/orchestration/test_graph.py`): existing success-path regression; factory requires both keyword-only dependencies; `BaseExceptionGroup` identity and original Phase 2 state reach the injected runtime handler exactly once; handler result `ingestion`/`failed` skips the success transition and ends the graph; real Chunk 57–61 composition of a single attributed group through LangGraph to `FAIL` / `INGESTION` / `FAILED`; multiple attributed failures and unattributed leaves still fail closed without a winner rule or success transition; direct non-group `InvalidRequestError` and `DependencyUnavailableError` propagate without calling the handler; unexpected post-Phase-2 phase/status combinations fail closed as `InvalidRequestError`. No live service. No Docker.
- Architecture test (`tests/architecture/test_langgraph_boundary.py`): updated for the injected runtime handler, `BaseExceptionGroup` catch, one Phase-2-specific `add_conditional_edges` seam, and continued prohibition of lower-level failure internals, agents, executor, infrastructure, ML, API, checkpointers, stores, and `create_app()` wiring.

Chunk 63 Regulatory Intelligence Agent application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_regulatory_constraint_inference.py`): a test-only structural fake satisfies async keyword-only `RegulatoryConstraintInferencePort.infer`; it does not inherit a production or infrastructure base class. Coverage includes exact normalized chunk-tuple identity, empty constraint tuple, one canonical `RegulatoryConstraint`, multiple constraints preserving order, canonical fields passing through unchanged, and sanitized `DependencyUnavailableError`. No HTTP, Docker, live Qdrant, or live LLM.
- Unit tests (`tests/unit/application/agents/test_regulatory_intelligence.py`): frozen/slots request preserves exact `DocumentVectorSearchQuery` identity; unrequested prompt/provider fields rejected; frozen/slots result; empty constraint tuple valid; one and many canonical `RegulatoryConstraint` values preserve order and identity; non-tuple and non-`RegulatoryConstraint` collections rejected. Concrete agent `name` is `AgentName.REGULATORY_INTELLIGENCE`; `run` awaits search exactly once with the supplied query; retrieved chunks reach inference by identity; inference is awaited exactly once; returned constraints are not reconstructed. Empty retrieval returns `constraints == ()` and does not invoke inference. Search `DependencyUnavailableError` propagates unchanged without calling inference. Inference `DependencyUnavailableError` propagates unchanged without fabricating empty success. Structural assignment to `AgentPort[RegulatoryIntelligenceRequest, RegulatoryIntelligenceResult]` is proven without inheritance. The agent does not mutate the request, retrieved chunks, or returned constraints.
- Architecture test (`tests/architecture/test_regulatory_intelligence_agent_boundary.py`): agent and inference-port modules import none of infrastructure/ML/API, LangGraph/LangChain, FastAPI/Starlette, Redis/SQLAlchemy/Qdrant, HTTP clients, n8n, OpenAI/Anthropic, or ML/LLM libraries. The port is a non-generic Protocol whose only public operation is async `infer` returning `tuple[RegulatoryConstraint, ...]`. No concrete provider/LLM implementation and no generic `LLMPort`. Agent public operations remain `name` and `run`. Constructor injects exactly `DocumentVectorSearchPort` and `RegulatoryConstraintInferencePort`. AST proves the agent does not construct `RegulatoryConstraint`, Qdrant search implementations, or LLM SDK types. No `Any`/`dict`/`Mapping` payload and no DAM-rule constants. `create_app()` and `graph.py` remain unwired. Production `langgraph` imports remain confined to the existing graph module. No live Qdrant or LLM tests and no Docker requirement. The orchestration-state agent-module inventory is updated to include `regulatory_intelligence.py` without weakening orchestration isolation.

Chunk 64 document query-text embedding application-boundary tests:

- Unit tests (`tests/unit/application/ports/test_document_query_embedding.py`): a test-only structural fake satisfies async `DocumentQueryEmbeddingPort.embed_query` without inheriting the Protocol. Coverage includes exact query-text identity with no prompt transformation; blank/whitespace-only `InvalidRequestError` without echoing the text; valid one- and many-element finite vectors; frozen/slots immutability; tuple-only vectors; rejection of lists, empty vectors, non-floats, NaN, and ±Infinity; unexpected extra fields; deterministic value equality; and sanitized `DependencyUnavailableError` that does not leak query text. No live embedding provider, HTTP, Docker, or LLM.
- Architecture test (`tests/architecture/test_document_query_embedding_boundary.py`): only `DocumentQueryEmbedding` and `DocumentQueryEmbeddingPort` are public production classes. The port is a non-generic Protocol whose only public operation is async `embed_query(self, query_text: str) -> DocumentQueryEmbedding`. The DTO has exactly `vector: tuple[float, ...]`. No provider implementation, generic `EmbeddingPort`/`LLMPort` hierarchy, infrastructure/API/ML imports, LangGraph/LangChain, Qdrant, LLM/embedding SDKs, HTTP clients, NumPy/DataFrame/raw bytes/path/URL surface, or `Any`/`dict`/`Mapping`. `create_app()`, `graph.py`, and `RegulatoryIntelligenceAgent` remain unwired. Production `langgraph` imports remain confined to the existing graph module. No live embedding provider is required.

Chunk 65 document vector-search query preparation tests:

- Unit tests (`tests/unit/application/orchestration/test_document_vector_search_query_preparation.py`): a test-only structural fake satisfies `DocumentQueryEmbeddingPort` without inheriting the Protocol. Coverage includes constructor injection of that fake; keyword-only async `prepare(*, query_text, limit) -> DocumentVectorSearchQuery`; exact query-text identity including surrounding whitespace with `embed_query` awaited exactly once; returned query using the exact embedding vector and caller-supplied limit; `DependencyUnavailableError` from embedding propagating unchanged with no fabricated search query; blank/whitespace `InvalidRequestError` from a conforming fake propagating unchanged; invalid `limit` preserving existing `DocumentVectorSearchQuery` `ValueError`/`TypeError` after embedding is still called; and deterministic value-equivalent results for equivalent inputs. No live embedding provider, search invocation, HTTP, Docker, or LLM.
- Architecture test (`tests/architecture/test_document_vector_search_query_preparation_boundary.py`): only `DocumentVectorSearchQueryPreparationService` is the public production class. Constructor injects exactly `DocumentQueryEmbeddingPort`. The only public operation is keyword-only async `prepare` returning `DocumentVectorSearchQuery`. AST proves `embed_query` then `DocumentVectorSearchQuery` construction, no `.search(...)`, no vector-member transformation, and no `try/except` fallback. Forbidden: infrastructure/API/ML imports, LangGraph/LangChain, Qdrant, LLM/embedding SDKs, HTTP clients, Redis/database, NumPy/DataFrame, `Any`/`dict`/`Mapping`, provider/model/prompt/config surface, generic RAG/embedding frameworks, Regulatory-agent invocation, and vector-search invocation. `create_app()`, `graph.py`, and `RegulatoryIntelligenceAgent` remain unwired. Production `langgraph` imports remain confined to the existing graph module. No live embedding provider is required.

Chunk 66 Regulatory Intelligence query execution tests:

- Unit tests (`tests/unit/application/orchestration/test_regulatory_intelligence_query_execution.py`): narrowly typed recording doubles satisfy the two constructor dependencies without provider implementations. Coverage includes constructor injection of exactly `DocumentVectorSearchQueryPreparationService` and `RegulatoryIntelligenceAgent`; keyword-only async `execute(*, query_text, limit) -> RegulatoryIntelligenceResult`; exact query-text and limit identity including surrounding whitespace with `prepare` awaited exactly once; `RegulatoryIntelligenceRequest` constructed with the exact prepared `DocumentVectorSearchQuery`; `run` awaited exactly once; the agent's `RegulatoryIntelligenceResult` returned unchanged; preparation `DependencyUnavailableError` propagating unchanged without calling the agent; agent `DependencyUnavailableError` propagating unchanged without fabricating empty constraints; empty `constraints=()` treated as legitimate success; and deterministic value-equivalent results for equivalent inputs. A focused real composition uses real `DocumentVectorSearchQueryPreparationService`, real `RegulatoryIntelligenceAgent`, and real `RegulatoryIntelligenceQueryExecutionService` behind fakes at `DocumentQueryEmbeddingPort`, `DocumentVectorSearchPort`, and `RegulatoryConstraintInferencePort`, proving query text + limit through embedding → preparation → execution → search → inference → canonical result, and empty retrieval skipping inference. No live Qdrant, embedding provider, or LLM is required.
- Architecture test (`tests/architecture/test_regulatory_intelligence_query_execution_boundary.py`): only `RegulatoryIntelligenceQueryExecutionService` is the public production class. Constructor injects exactly the published query-preparation service and Regulatory Intelligence Agent. The only public operation is keyword-only async `execute` returning `RegulatoryIntelligenceResult`. AST proves `prepare` then `RegulatoryIntelligenceRequest` then `run`, with no `embed_query`, no `.search(...)`, no inference call, and no `try/except` fallback. Forbidden: infrastructure/API/ML imports, LangGraph/LangChain, Qdrant, LLM/embedding SDKs, HTTP clients, Redis/database, NumPy/DataFrame, `Any`/`dict`/`Mapping`, generic RAG/executor/registry, and direct embedding/search/inference. `create_app()` and `graph.py` remain unwired. Production `langgraph` imports remain confined to the existing graph module. The orchestration-state class inventory includes `RegulatoryIntelligenceQueryExecutionService`. No live Qdrant/LLM/embedding service is required.

Chunk 67 Regulatory Intelligence runtime composition-root tests:

- Unit tests (`tests/unit/api/test_regulatory_intelligence_composition.py`): structural fakes satisfy the three injected application ports without production provider classes. Coverage includes keyword-only synchronous `build_regulatory_intelligence_query_execution`; return type `RegulatoryIntelligenceQueryExecutionService`; wiring of real `DocumentVectorSearchQueryPreparationService` and `RegulatoryIntelligenceAgent` to the exact supplied fakes; construction-time side-effect freedom (`embed_query` / `prepare` / `search` / `infer` / `run` / `execute` not called); built-service behavioral proof of query text + limit through embedding → preparation → execution → search → inference → canonical `RegulatoryIntelligenceResult`; empty retrieval skipping inference; and embedding `DependencyUnavailableError` propagating unchanged without builder-level handling. No live Qdrant, embedding provider, or LLM is required.
- Architecture test (`tests/architecture/test_regulatory_intelligence_composition_boundary.py`): the builder lives in `energy_trading.api.composition`. It injects exactly the three published application ports and returns `RegulatoryIntelligenceQueryExecutionService`. AST proves construction of the three published application objects with no runtime method calls, no generic container/registry/factory, no vendor SDKs, no settings/environment access, and no FastAPI routes. Application does not import the builder. `create_app()` and `graph.py` remain unwired. HTTP-transport API modules remain separately proven unwired. Production `langgraph` imports remain confined to the existing graph module. No live Qdrant/LLM/embedding service is required.

## CI expectations (future)

- Unit + architecture tests on every change once a runner exists.
- Integration tests gated on optional services.
- No secret-bearing `.env` in CI; use `.env.example` values and fixtures.
